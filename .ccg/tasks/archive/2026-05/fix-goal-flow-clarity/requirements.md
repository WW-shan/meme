# Requirements

User feedback:

- Do not open a new goal/task node while the previous verified work is still unarchived, uncommitted, or unpushed.
- Make the goal-flow plan explicit enough that completion, archive, commit, and push are treated as gates before follow-up work.

Acceptance:

- Add a durable instruction outside `docs/goals/`.
- Do not edit protected goal documents.
- Close the already completed work before starting the next research task.
