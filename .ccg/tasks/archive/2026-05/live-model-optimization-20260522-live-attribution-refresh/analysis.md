# Analysis

## Current Authoritative State

- Worktree started clean at commit `1a461fc` (`origin/main`).
- No active CCG task with a `task.json` existed before this round; `.ccg/tasks/fix-github-actions-unittest` is an empty directory.
- `.ccg/spec/` is absent.
- `docs/goals/**` has no working-tree or staged changes and is protected for this round.
- Previous task `live-model-optimization-20260522-active-flow-replay` is archived, committed, and pushed.

## Latest Live State

- `data/bot_state.json` has no open positions (`positions={}`).
- `data/paper_trades.jsonl` includes additional real trades after the earlier conditional-exit attribution window, including `🆙`, `披风`, `AUCA`, and `币安队长` losses after `人间半夏小得盈满`.
- Recent live signal audit is still active through `2026-05-22 06:49` with rejected `C4` decisions, so the bot/listener data stream is current.

## Prior Evidence

The active-flow total-buys quick-profit replay was rejected. It improved headline validation/final profit but over-expanded trades and degraded win rate/stress, so the next defensible path is not another broad quick-profit entry overlay.

Earlier conditional-exit work identified plausible buckets (`mfe_then_giveback`, `dead_flow_timeout`, `entry_slippage_failure`), but the previous live attribution was generated at `2026-05-21T15:24:45Z` and does not include the newest real losses.

## Local Codex Read

The repo has existing support/feasibility scripts for conditional exit and post-target/dead-flow evidence, but the previous `live_attribution.json` appears to be a static report, not a reusable refreshed report generated by a current CLI. A reusable read-only live trade attribution probe would make the next research step more evidence-driven and avoid manual cherry-picking of the latest losses.

## Proposed Experiment

1. Add or use a read-only live trade attribution path that reconstructs real open/close pairs from `data/paper_trades.jsonl` and links each trade to path metrics from lifecycle data.
2. Classify trades into conservative failure buckets using only signal/entry-to-exit path information:
   - `profitable_exit`
   - `mfe_then_giveback`
   - `dead_flow_timeout`
   - `entry_slippage_failure`
   - `stop_first_after_entry`
   - `unprofitable_other`
3. Generate a refreshed report under `docs/research/20260522-live-trade-attribution-refresh/` with `live_switch_evidence=false` and `safe_for_live_switch=false`.
4. Decide whether a replay-integrated conditional exit or veto experiment has enough live support for the next round. Do not switch live from this attribution.

## Risk Assessment

- Complexity: M (new/reused research reporting surface + tests/reports/docs).
- Risk: medium (classification/reporting could bias future decisions if not conservative; no live runtime change planned).
- Requires current Codex local analysis plus external Claude analysis before implementation/evidence generation, and local plus Claude review before closure.

## External Claude Analysis Status

Pending. Per user instruction, this round will give Claude a longer timeout and will not start another task while waiting.

## External Claude Analysis — 2026-05-22

- Claude confirmed the right next move is evidence discipline after the rejected active-flow quick-profit replay, not another static overlay.
- Correction to local pre-read: the existing 20260521 live attribution already covers the latest closed paper trade (`币安队长`, closed 2026-05-21 20:42 local); the refresh still adds value only if it makes attribution reproducible from raw live files and records current no-new-trade status.
- Required evidence: causal-only signal-time fields, explicit MFE/MAE labels as diagnostic-only, bucket BNB PnL, near vs primary recomputed from manifest-like thresholds, lifecycle/flow coverage, representative token/time samples, and `read_only=true` / `live_switch_evidence=false`.
- Main pitfalls: promoting 18-trade diagnostics to live rules, using post-entry path fields as deployable features, treating missing flow defaults as real zero flow, or repeating previously rejected static gates.
- Decision criteria: default `NO_GO_FOR_LIVE_SWITCH`; only write a next-round replay hypothesis if a bucket reaches >=7 same-shape trades plus causal discriminators, but still no runtime/model/config change in this round.
