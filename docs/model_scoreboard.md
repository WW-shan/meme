# Model Scoreboard

This file records accepted and rejected model candidates for live FourMeme trading. Selection is based on live-sized replay with 10% position sizing, gas costs, current execution delay assumptions, walk-forward checks, and stress replay.

## Accepted Baseline

| Model | Status | Threshold | Trades | Net Return | Net Profit BNB | Win Rate | Max DD | WF Worst Return | WF Worst DD | Reason |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `data/models/20260519_v95_v84_selective_nearmiss_gate` + `entry_value` / primary `min_entry_score=35` / near rescue `prob>=0.94, score>=32, volume_30s>=1.25, price_volatility>=0.08` | accepted/canary candidate | `0.98` primary, `0.94` near | `44` | `511.4778%` | `0.03067113` | `79.5455%` | `-8.0587%` | `111.8292%` | `-12.7578%` | Live-first ERNIE/BNBROCK near-miss attribution showed a narrow missed-runner pocket rather than a global threshold problem. SmartSearch-backed selective abstention/meta-label research led to a conditional near-threshold gate that preserves v84's primary `0.98` buy threshold and only rescues the `0.94-0.98` band when entry value, 30s volume, volatility, and age gates all pass. Validation improved by `0.00056397` BNB without increasing trades; sealed final improved by `0.00210174` BNB with only one extra trade and WF worst return improving from `82.8426%` to `111.8292%`. Because the validation rule was under-discriminated (`144/144` grid points passed, `near_episode_count=2`) and the final uplift is one trade, treat v95 as a canary candidate, not conclusive proof of a durable edge. |
| `data/models/20260516_v67_v65_thr9715_tr35_12` + `entry_value` / `min_entry_score=40` | accepted/live baseline | `0.9715` | `65` | `189.6565%` | `0.01137289` | `67.6923%` | `-15.6950%` | `25.2537%` | `-17.3727%` | Same v67 buy/sell weights, hold75 exits, and 10% sizing; accepted on the 2026-05-18 current-data rerun because the PredReturn gate beat the ungated hold75 baseline on validation, final, walk-forward, win rate, drawdown, and harsh stress. Same-data final baseline was `187.9662%` return, `61.9565%` win rate, `-21.5063%` max DD, and `22.2034%` WF worst return. |
| `data/models/20260516_v67_v65_thr9715_tr35_12` | superseded live baseline | `0.9715` | `89` | `194.7110%` | `0.01219864` | `62.9213%` | `-21.2987%` | `22.8967%` | `-21.2987%` | Latest-calibrated v67 runtime profile with `max_hold_seconds=300`, `min_policy_hold_seconds=75`, `stop_loss=-0.25`, and `trailing=35%/12%`. Same v67 buy/sell weights and 10% sizing; accepted after 2026-05-17 live execution calibration because validation return, win rate, walk-forward, and harsh stress improved over hold60, while final return and win rate also improved with controlled drawdown. |

## Accepted Offline Candidate

| Model | Status | Threshold | Trades | Net Return | Net Profit BNB | Win Rate | Max DD | WF Worst Return | WF Worst DD | Reason |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---|
| `data/models/20260518_v84_v83_method_all_data_prod` + `entry_value` / `min_entry_score=35` / `min_entry_volume_30s=1.5` / `entry_price_protection=0.25` / `stop_loss=-0.18` / `min_policy_hold=45` / `max_hold=560` | accepted/offline, live switch pending | `0.98` | `43` | `476.4288%` | `0.02856940` | `79.0698%` | `-7.8221%` | `82.8426%` | `-13.0626%` | All-data production fit of the v83 method with 10% sizing, current live execution delays/costs, and the v83 runtime exit profile. A 2026-05-18 live-runner attribution sweep widened entry protection from `0.18` to `0.25`, reduced `min_policy_hold_seconds` from `75` to `45`, then swept `max_hold_seconds` through `420`, `480`, `540`, `560`, `580`, and `600`. The `560s` point now wins: it improves final return, profit, walk-forward, harsh friction, and harsh execution over `540s` while keeping the same max drawdown. The calibration threshold from the production fit was `0.993607752039693`, but the deployable runtime threshold is re-anchored to `0.98` because that replay is strictly better. |
| `data/models/20260518_v83_targethit60_riskadjusted_entryv` + `entry_value` / `min_entry_score=35` / `min_entry_volume_30s=1.5` / `stop_loss=-0.18` | accepted/offline, live switch pending | `0.98` | `61` | `307.6195%` | `0.01844662` | `70.4918%` | `-6.5848%` | `23.7401%` | `-14.3655%` | 60% target-hit barrier model trained from live runner evidence, with risk-adjusted entry value gate, 10% sizing, and a tightened stop after exit tuning. It beats the current live v67 baseline on final return, net profit, win rate, max drawdown, WF worst drawdown, and stress replay (`101.3053%` harsh friction, `76.9625%` harsh execution), while WF worst return remains positive and close to v67. Not live-switched in this pass because the user asked not to restart the bot. |

## Live Runtime Guard Updates

| Date | Model Context | Status | Evidence | Decision |
|---|---|---|---|---|
| 2026-05-23 | `data/models/20260519_v95_v84_selective_nearmiss_gate` live canary, collector reactivation age guard | accepted/runtime correctness guard | Live round `live-model-optimization-20260523-0706-runtime-cadence-refresh` found that `背手负书` (`0x698cf962a0F972962c7Aa85728e41EAeF3C94444`) was analyzed after `07:13` with `token_age_seconds=107/175`, even though bot-local lifecycle flushes for the same address showed earlier create times at `03:26:22` and `06:44:45`. Root cause: flushed tokens rehydrated from metadata lost original `create_timestamp`/`create_block`, so `MemeBot`'s max-age gate could treat old reactivated tokens as young. | Keep the v95 canary model, thresholds, `.env`, sizing, and running bot process unchanged. Accept a collector metadata fix: `src/data/collector.py` now persists original `createTimestamp`/`createBlock` and restores them on metadata rehydration while keeping `last_update` on the new event. Regression tests cover direct reactivation, metadata-index roundtrip, and legacy v1 fallback. Verification: `python -m unittest tests.model.test_data_collector_reactivation`, related collector/runtime tests, `python -m unittest tests.core.test_hybrid_requirements_contract`, and `python -m unittest discover` (`746` tests, `1` skipped) passed. |
| 2026-05-22 | `data/models/20260519_v95_v84_selective_nearmiss_gate` live canary, no model/env/sizing change | accepted/runtime state-persistence guard | Research `docs/research/20260522-live-zero-balance-state-guard/summary.md`. Fresh live trades after `2026-05-22 13:00` showed one operational liquidation (`华尔街瞎报`, `APP_STOP_LIQUIDATION` from restart), two weak/late losing exits (`FIGHT`, `PORA`), and one zero-balance cleanup defect (`吃饱饱赚饱饱`). `PORA` confirmed the new peak-relative attribution was useful (`lifecycle_price_from_peak_pct=-0.6311052905052421`). `吃饱饱赚饱饱` opened at `13:42:48`, attempted `TIME_EXIT` at `13:52:09`, then logged token balance `0` and removed the in-memory position, but `data/bot_state.json` still listed the token and no close/audit row existed. | Keep v95 canary model, thresholds, `.env`, and sizing unchanged. Accept a runtime state-persistence guard: `src/trader/bot.py` now writes `POSITION_ZERO_BALANCE_REMOVED`, adds zero-balance sell removals to `closed_tokens`, and saves state immediately, preventing stale position resurrection on restart while preserving audit attribution without creating a fake `CLOSE` trade row. Verification: TDD red/green on zero-balance sell persistence/audit coverage; `python -m unittest tests.core.test_hybrid_requirements_contract` (`82` tests OK), `python -m unittest discover` (`739` tests OK, `1` skipped), and `git diff --check` passed. |
| 2026-05-22 | `data/models/20260519_v95_v84_selective_nearmiss_gate` live canary, no model/env/sizing change | accepted/runtime observability update | Research `docs/research/20260522-late-entry-attribution-visibility/summary.md`. Fresh live state had bot and collector running, `0` open positions, balance `0.003455339585131376` BNB, and no new real close after `币安队长` at `2026-05-21 20:42:26.327946`. Recent high-probability rejects such as `Bsteroid`, `飞向未来`, `Shop with Pizza`, and `BIZZA` were mostly signaled after their local lifecycle peak, then faded or ended near/below first observed price, so they are not clean evidence for entry widening. | Keep v95 canary model, thresholds, `.env`, and sizing unchanged. Add peak-relative lifecycle attribution fields to fast-status buy/audit payloads: `lifecycle_price_current`, `lifecycle_price_first`, `lifecycle_price_peak`, `lifecycle_price_from_first_pct`, and `lifecycle_price_from_peak_pct`. Verification: TDD red/green on fast-status open and unsupported-quote reject paths; `python -m unittest tests.core.test_hybrid_requirements_contract` (`81` tests OK), `python -m unittest discover` (`739` tests OK, `1` skipped), and `git diff --check` passed. |
| 2026-05-22 | `data/models/20260519_v95_v84_selective_nearmiss_gate` live canary, no model/env/sizing change | accepted/runtime safety guard | Research `docs/research/20260522-live-unsupported-quote-guard/summary.md`; evidence `docs/research/20260522-live-unsupported-quote-guard/quote_guard_evidence.json`; external Claude session `17705108-23ea-47b8-802f-0584235cedeb`. Since `2026-05-21 20:00:00`, live signal audit had `7743` decisions, `4` queued buys, and `2` buy receipt reverts. The two filled examples (`AUCA`, `币安队长`) had native quote `0x0000000000000000000000000000000000000000`; the two reverted examples (`WHITE HOUSE`, `HUNTER`) had quote `0x8d0D000Ee44948FC98c9B98A4FA4921476f08B0d`. HUNTER fast status was fresh (`stale≈0.012s`, `chain_lag≈2.815s`), and latest same-shape `eth_call` reverted with `ERC20: insufficient allowance`, pointing to unsupported ERC20 quote asset rather than listener lag. | Keep v95 canary model and runtime thresholds unchanged, but accept the native-quote execution guard. `src/core/trader.py` now rejects unsupported non-native quote assets in helper status and exposes `check_token_quote_supported`; `src/trader/bot.py` validates quote support before fast-lifecycle buy submission and records `BUY_NOT_READY` with `token_quote` instead of sending a reverting tx. Verification: TDD red/green for quote-guard regressions, structured quote-helper failure, and native fast-path happy-path coverage; `python -m unittest tests.core.test_hybrid_requirements_contract` (`80` tests OK), and `python -m unittest discover` (`739` tests OK, `1` skipped). |

## Rejected Candidates

| Date | Model | Status | Threshold | Trades | Net Return | Net Profit BNB | Win Rate | Max DD | WF Worst Return | Stress Summary | Decision |
|---|---|---|---:|---:|---:|---:|---:|---:|---:|---|---|
| 2026-05-23 | `data/models/20260519_v95_v84_selective_nearmiss_gate` post-APYX high-PredReturn token `5` rejected cluster refresh | rejected/read-only diagnostic | `0.98` primary, `0.94` near, no runtime overlay | trades `0`; since `2026-05-23 06:09:33`: `1` per-token candidate from `5` signal decisions (`4` duplicates dropped); classes `flat_timeout=1`; low-volume per-token candidates `0` at actual `min_prob=0.94` | n/a | n/a | n/a | n/a | n/a | Reports `data/replay_reports/time_to_barrier_probe_20260523_0624_since_060933.json`, `data/replay_reports/low_volume_breakout_probe_20260523_0624_since_060933_prob94.json`, and `data/replay_reports/support_action_policy_20260523_0624_since_060933.json`; `live_switch_evidence=false`, `requires_replay_before_live_change=true`. There was no paper trade after `龙爪` closed at `2026-05-22 18:17:47.929280`. Token `5` was a high-PredReturn but low-probability disagreement: the selected rejected signal had `prob=0.583570`, `PredReturn=123.900340`, `volume_30s=20.154845`, `price_volatility=0.078165`, and age `268s`. Its 600s path started at/near the local peak and faded (`MFE=-2.82%`, `MAE=-6.14%`, no `+25`, `+60`, `-18`, or `-25` hit). The low-volume breakout report found `0` candidates, and support policy stayed `probe_only_replay_required` with `positive_candidates=0` and `eligible_rule_results=0`. | Do not switch live, loosen the primary `0.98` gate, lower the `0.94` near-rescue floor, add a raw high-PredReturn rescue, change `.env`, resize, or restart the bot. Treat this as confirmation that high `PredReturn` alone is not a safe entry signal when buy probability is far below the rescue band. |
| 2026-05-23 | `data/models/20260519_v95_v84_selective_nearmiss_gate` post-quiet APYX rejected-signal cluster refresh | rejected/read-only diagnostic | `0.98` primary, `0.94` near, no runtime overlay | trades `0`; since `2026-05-23 05:48:14`: `1` per-token candidate from `15` signal decisions (`14` duplicates dropped); classes `flat_timeout=1`; low-volume per-token candidates `0` at actual `min_prob=0.94` | n/a | n/a | n/a | n/a | n/a | Reports `data/replay_reports/time_to_barrier_probe_20260523_0609_since_054814.json`, `data/replay_reports/low_volume_breakout_probe_20260523_0609_since_054814_prob94.json`, and `data/replay_reports/support_action_policy_20260523_0609_since_054814.json`; `live_switch_evidence=false`, `requires_replay_before_live_change=true`. There was no paper trade after `龙爪` closed at `2026-05-22 18:17:47.929280`. `APYX` was the first post-quiet token to clear the pre-inference gate, but its best deduped rejected signal was still a near-threshold negative-PredReturn/low-volume skip (`prob=0.953466`, `PredReturn=-3.201985`, `volume_30s=0.376238`, `price_volatility=0.086238`, age `71s`). The 600s path stayed below the target barrier (`MFE=9.49%`, `MAE=-1.73%`, no `+25`, `-18`, or `-25` hit), the low-volume breakout report found `0` qualifying candidates, and the support policy stayed `probe_only_replay_required` with `positive_candidates=0` and `eligible_rule_results=0`. | Do not switch live, loosen `PredReturn`, relax `MIN_ENTRY_VOLUME_30S=1.5`, widen max entry age, change `.env`, resize, or restart the bot. Treat APYX as confirmation that the post-quiet signal was a correct skip rather than a missed-runner or gate-relaxation candidate. |
| 2026-05-23 | `data/models/20260519_v95_v84_selective_nearmiss_gate` 03:38 rejected-signal cluster refresh (`新时代`, `加密时代`, `ERA`, `A new era`, `Kevun`) | rejected/read-only diagnostic | `0.98` primary, `0.94` near, no runtime overlay | trades `0`; since `2026-05-23 03:38:54`: `14` per-token candidates from `440` signal decisions (`426` duplicates dropped); classes `fast_profit_then_collapse=1`, `flat_timeout=9`, `stop_first=4`; low-volume per-token candidates `1` at actual `min_prob=0.94` | n/a | n/a | n/a | n/a | n/a | Reports `data/replay_reports/time_to_barrier_probe_20260523_0400_since_033854.json`, `data/replay_reports/low_volume_breakout_probe_20260523_0400_since_033854_prob94.json`, and `data/replay_reports/support_action_policy_20260523_0400_since_033854.json`; `live_switch_evidence=false`, `requires_replay_before_live_change=true`. There was no paper trade after `龙爪` closed at `2026-05-22 18:17:47.929280`. `新时代` `0xBA39...` was a high-probability `stop_first` with `prob=0.986387`, `PredReturn=-2.336308`, `volume_30s=2.062405`, `price_volatility=0.117122`, MFE `831.61%`, MAE `-22.64%`, and `time_to_minus_18=3.246796s`; this is confirmation that the `PredReturn` veto saved a collapse-first path, not a loosen-gate candidate. `A new era` `0x36eb...` and `加密新时代` `0x0d91...` were additional high-probability `stop_first` confirmations (`PredReturn=16.179813` / `15.957627`) with MAE `-48.03%` / `-32.77%` and `time_to_minus_18=29.564285s` / `0.374778s`. The low-volume breakout report found one `ERA` `fast_profit_then_stop` watchpoint (`prob=0.983653`, `PredReturn=1.945431`, `volume_30s=1.062422`, `price_volatility=0.067881`, MFE `33.24%`, MAE `-22.90%`, `+25` in `3.37386s`, `-18` in `16.37386s`), and the support policy still returned `decision=probe_only_replay_required` with `positive_candidates=1`, `eligible_rule_results=0`. `Kevun` appeared as a `fast_profit_then_collapse` watchpoint (`prob=0.979394`, `PredReturn=-0.523554`, `volume_30s=0.974844`, `price_volatility=0.055464`, `+25` in `15.916891s`, `-18` in `59.916891s`), so the new watchpoints remain replay-only. | Do not switch live, loosen `PredReturn`, relax `MIN_ENTRY_VOLUME_30S=1.5`, enable a live quick-TP path, change `.env`, resize, or restart the bot. Treat the `新时代` 831.61% MFE stop-first as validation that the current gate vetoed a collapse-first path, not as a missed opportunity. Keep `ERA` and `Kevun` as replay-only watchpoints for future quick-TP / path-state research. |
| 2026-05-23 | `data/models/20260519_v95_v84_selective_nearmiss_gate` 03:26 rejected-signal cluster refresh (`MORE`, `快乐`, `少即是多`, `背手负书`) | rejected/read-only diagnostic | `0.98` primary, `0.94` near, no runtime overlay | trades `0`; since `2026-05-23 03:26:07`: `7` per-token candidates from `123` signal decisions (`116` duplicates dropped); classes `fast_profit=1`, `flat_timeout=4`, `stop_first=2`; low-volume per-token candidates `0` at actual `min_prob=0.94` | n/a | n/a | n/a | n/a | n/a | Reports `data/replay_reports/time_to_barrier_probe_20260523_0328_since_032607.json`, `data/replay_reports/low_volume_breakout_probe_20260523_0328_since_032607_prob94.json`, and `data/replay_reports/support_action_policy_20260523_0328_since_032607.json`; `live_switch_evidence=false`, `requires_replay_before_live_change=true`. There was no paper trade after `龙爪` closed at `2026-05-22 18:17:47.929280`. The high-probability `MORE` candidate (`prob=0.988842`, `PredReturn=-7.914040`, `volume_30s=9.426593`, `price_volatility=0.242573`) was rejected by `PredReturn` and then classified `stop_first` with MFE `19.24%`, MAE `-64.20%`, and `time_to_minus_18=3.94187s`. The high-probability low-volume `背手负书` candidate was rejected by `entry_volume_30s_below_min` despite positive PredReturn (`prob=0.982211`, `PredReturn=5.465615`, `volume_30s=0.990853`, `price_volatility=0.085898`), then classified `stop_first` with MAE `-22.40%` and `time_to_minus_18=25.080009s`. The only `fast_profit` was a different low-probability `背手负书` candidate (`prob=0.628922`, `volume_30s=0.392475`, `+25` in `82.674326s`), so it is a low-probability path-state watchpoint rather than a current primary or near-rescue gate miss. Support policy stayed inconclusive with `decision=probe_only_replay_required`, policy counts `quick_take_profit=1` / `skip=6`, `positive_candidates=1`, `negative_candidates=6`, and `eligible_rule_results=0`. | Do not switch live, loosen `PredReturn`, relax volume or volatility gates, change `.env`, resize, or restart the bot. Treat this as fresh confirmatory evidence for keeping the current PredReturn and entry-liquidity/volatility gates, plus a replay-only low-probability missed-runner watchpoint for future path-state research. |
| 2026-05-23 | `data/models/20260519_v95_v84_selective_nearmiss_gate` `pocky` high-probability low-PredReturn stop-first confirmation | rejected/read-only diagnostic | `0.98` primary, `0.94` near, no runtime overlay | trades `0`; since `2026-05-23 02:52:05`: `1` per-token candidate from `30` signal decisions (`29` duplicates dropped); low-volume per-token candidates `0` at actual `min_prob=0.98` | n/a | n/a | n/a | n/a | n/a | Reports `data/replay_reports/time_to_barrier_probe_20260523_0307_since_025205.json`, `data/replay_reports/low_volume_breakout_probe_20260523_0307_since_025205_prob94.json`, and `data/replay_reports/support_action_policy_20260523_0307_since_025205.json`; `live_switch_evidence=false`, `requires_replay_before_live_change=true`. There was no paper trade after `龙爪` closed at `2026-05-22 18:17:47.929280`. The single deduped `pocky` candidate was rejected by `PredReturn` despite high primary probability (`prob=0.984230`, `PredReturn=0.447972`, `volume_30s=2.700990`, `price_volatility=0.185695`) and then classified `stop_first`: MFE `-6.92%`, MAE `-34.47%`, and `time_to_minus_18=70.102351s`. The low-volume breakout report is only a null observation (`0` candidates at actual `min_prob=0.98`), not filter validation. Support policy stayed inconclusive with `decision=probe_only_replay_required`, `positive_candidates=0`, and `eligible_rule_results=0`. | Do not switch live, loosen `PredReturn`, relax volume gates, change `.env`, resize, or restart the bot. Treat this as an n=1 confirmatory rejection that supports keeping the current PredReturn gate, not as a trend or deployable policy change. |
| 2026-05-23 | `data/models/20260519_v95_v84_selective_nearmiss_gate` near-threshold / low-volume reject cluster refresh (`马万亿`, `Spark`) | rejected/read-only diagnostic | `0.98` primary, `0.94` near, no runtime overlay | trades `0`; since `2026-05-23 02:00:37`: `5` per-token candidates from `45` signal decisions; since `2026-05-23 02:40:40`: `2` per-token candidates from `19` signal decisions; low-volume per-token candidates `1` | n/a | n/a | n/a | n/a | n/a | Reports `data/replay_reports/time_to_barrier_probe_20260523_0245_since_020037.json`, `data/replay_reports/time_to_barrier_probe_20260523_0245_since_024040.json`, `data/replay_reports/low_volume_breakout_probe_20260523_0245_since_020037_prob94.json`, `data/replay_reports/low_volume_breakout_probe_20260523_0245_since_024040_prob94.json`, `data/replay_reports/support_action_policy_20260523_0245_since_020037.json`, and `data/replay_reports/support_action_policy_20260523_0245_since_024040.json`; `live_switch_evidence=false`, `requires_replay_before_live_change=true`. There was no paper trade after `龙爪` closed at `2026-05-22 18:17:47.929280`. `马万亿` was a near-threshold reject (`prob=0.972204`, `PredReturn=1.031596`, `volume_30s=0.669665`) with path MFE `17.58%`, MAE `-0.54%`, but no `+25` or stop barrier within the probe horizon. `Spark` first appeared as a primary-probability low-volume reject at `02:45:20` (`prob=0.981934`, `PredReturn=5.062587`, `volume_30s=0.626634`) with path MFE `11.96%` and no barrier hit. A second deduped low-volume `Spark` signal at `02:45:27` (`prob=0.981487`, `PredReturn=3.843595`, `volume_30s=1.020693`) was classified `low_volume_flat` with MFE `1.32%` and MAE `-7.11%`. Support policy stayed `probe_only_replay_required` with `positive_candidates=0` and `eligible_rule_results=0`. | Do not switch live, loosen `PredReturn`, relax volume gates, change `.env`, resize, or restart the bot. Treat this as fresh negative evidence that the current near-threshold and volume gates are still doing useful filtering; the path peaks are watchpoints for future replay-only candidate policies, not live deployment evidence. |
| 2026-05-23 | `data/models/20260519_v95_v84_selective_nearmiss_gate` post-quiet-window flat-timeout signal refresh / 信念驱动 check | rejected/read-only diagnostic | `0.98` primary, `0.94` near, no runtime overlay | trades `0`; since `2026-05-23 01:29:00`: `3` per-token candidates; current node `0`; low-volume slices `0` | n/a | n/a | n/a | n/a | n/a | Reports `data/replay_reports/time_to_barrier_probe_20260523_0145_since_012900.json`, `data/replay_reports/time_to_barrier_probe_20260523_0145_current_node.json`, `data/replay_reports/low_volume_breakout_probe_20260523_0145_since_012900_prob94.json`, `data/replay_reports/low_volume_breakout_probe_20260523_0145_current_node_prob94.json`, `data/replay_reports/support_action_policy_20260523_0145_since_012900.json`, and `data/replay_reports/support_action_policy_20260523_0145_current_node.json`; `live_switch_evidence=false`, `requires_replay_before_live_change=true`. There was no paper trade after `龙爪` closed at `2026-05-22 18:17:47.929280`. Since the last public commit window (`2026-05-23 01:29:00`), the signal stream had `17` rejected decisions concentrated in `活得好好的我艹你们冯` (`7`), `信念驱动` (`5`), and `CDOGEL` (`5`); `信念驱动` contributed all `5` `prob>=0.94` decisions, but the time-to-barrier probe emitted only `3` per-token candidates after dedupe and all were `flat_timeout`. The support policy stayed `probe_only_replay_required` with `eligible_rule_results=0`, `positive_candidates=0`, and only the `low_prob_hard_abstain` abstention rule selected `2` negatives. The low-volume breakout probe found `0` qualifying candidates. | Do not switch live, loosen `PredReturn`, relax volume gates, change `.env`, resize, or restart the bot. Treat this as a post-commit signal-diversity diagnostic that does not justify changing current filters. |
| 2026-05-23 | `data/models/20260519_v95_v84_selective_nearmiss_gate` quiet-window diagnostic / RIP星星 stop-first check | rejected/read-only diagnostic | `0.98` primary, `0.94` near, no runtime overlay | since `2026-05-23 01:10:33`: `1` per-token candidate; since `2026-05-23 00:49:37`: `2` per-token candidates | n/a | n/a | n/a | n/a | n/a | Reports `data/replay_reports/time_to_barrier_probe_20260523_0123_since_011033.json`, `data/replay_reports/time_to_barrier_probe_20260523_0123_since_004937.json`, `data/replay_reports/low_volume_breakout_probe_20260523_0123_since_011033_prob94.json`, `data/replay_reports/low_volume_breakout_probe_20260523_0123_since_004937_prob94.json`, `data/replay_reports/support_action_policy_20260523_0123_since_011033.json`, and `data/replay_reports/support_action_policy_20260523_0123_since_004937.json`; `live_switch_evidence=false`, `requires_replay_before_live_change=true`. After two no-op refreshes, the next window was not quiet: `RIP星星` generated `31` signal decisions since `01:10:33` with `17` `prob>=0.94`, but the only per-token candidate was `stop_first` with `pred_return=0.2473`, `mae=-51.62%`, and `time_to_minus_18=19.875841s`. Since the last public commit, the signal stream was concentrated in `RIP星星` and `Pepe4Dividends`; support policy remained `probe_only_replay_required` with `eligible_rule_results=0`, while `high_prob_volume_volatility` and `high_prob_low_toxic_overlap` each selected `RIP星星` but had `0` positives. | Do not switch live, loosen `PredReturn`, relax volume gates, change `.env`, resize, or restart the bot. Treat this as a quiet-window / signal-diversity diagnostic, not as live deployment evidence. |
| 2026-05-23 | `data/models/20260519_v95_v84_selective_nearmiss_gate` post-midnight rejected-signal micro-window quick-TP check | rejected/read-only diagnostic | `0.98` primary, `0.94` near, no runtime overlay | since `2026-05-23 00:29:33`: `6` per-token candidates; current node `5`; low-volume slices `0` | n/a | n/a | n/a | n/a | n/a | Reports `data/replay_reports/time_to_barrier_probe_20260523_0035_since_002933.json`, `data/replay_reports/time_to_barrier_probe_20260523_0035_current_node.json`, `data/replay_reports/low_volume_breakout_probe_20260523_0035_since_002933_prob94.json`, `data/replay_reports/low_volume_breakout_probe_20260523_0035_current_node_prob94.json`, `data/replay_reports/support_action_policy_20260523_0035_since_002933.json`, and `data/replay_reports/support_action_policy_20260523_0035_current_node.json`; `live_switch_evidence=false`, `requires_replay_before_live_change=true`. There was no paper OPEN/CLOSE after `龙爪` closed at `2026-05-22 18:17:47.929280`. The fresh micro-window found one `BINANCEFRIDAYMEME` fast-profit miss rejected by `pred_return_below_min`: `prob=0.984846`, `PredReturn=-0.4963`, `volume_30s=1.8528`, `price_volatility=0.1344`, `+25` in `22.176176s`, and `+60` in `90.176176s`. The support action policy remained in `probe_only_replay_required`: no positive-action rule produced an eligible result (`eligible_rule_results=0`); only the `low_prob_hard_abstain` abstention rule selected `3`/`2` candidates with `0` positives. This miss is a model-prediction error rather than a simple threshold gap because `prob=0.984846` and `PredReturn=-0.4963` would also fail the existing near-threshold rescue clauses. The low-volume breakout probe found no qualifying candidates. | Do not switch live, loosen `PredReturn`, relax volume gates, change `.env`, resize, or restart the bot. Treat this as another replay-input watchpoint for a quick-TP / collapse-aware candidate policy, not as live deployment evidence. |
| 2026-05-22 | `data/models/20260519_v95_v84_selective_nearmiss_gate` wide-window rejected-signal quick-TP support gate | rejected/read-only support gate | `0.98` primary, `0.94` near, no runtime overlay | `4156` rejected-signal candidates; low-volume slice `541` | n/a | n/a | n/a | n/a | n/a | Research `docs/research/20260522-wide-window-quick-tp-support/summary.md`; reports `data/replay_reports/time_to_barrier_probe_20260522_2015_since_7d_recent4.json`, `data/replay_reports/support_action_policy_20260522_2015_since_7d_recent4.json`, and `data/replay_reports/low_volume_breakout_probe_20260522_2015_since_7d_recent4_prob94.json`; `live_switch_evidence=false`, `requires_replay_before_live_change=true`. The recent live task window had only `4` rejected-signal candidates, all skip. The 7-day recent-lifecycle window scored `97987` signal decisions into `4156` per-token candidates: `fast_profit=255`, `fast_profit_then_collapse=405`, `slow_runner=48`, `flat_timeout=2019`, `stop_first=927`, and `missing_path=502`, with policy counts `quick_take_profit=660`, `conditional_slow_hold=48`, and `skip=3448`. Six support rules became eligible on the wide window, led by `v95_like_pred_rescue` (`14` selected, `9` positives, `64.29%` precision), but broader rules remained noisy (`33.53%`-`46.09%` precision). Low-volume evidence stayed mixed: `68` runners and `108` fast-profit-then-stop against `268` fakeouts and `95` flats. | Do not switch live, loosen thresholds, relax volume, change `.env`, resize, or restart the bot. This round promotes the next research direction to a replay-integrated quick-TP / collapse-aware candidate policy using v95/v84 as the primary candidate generator, with strict trade-count, walk-forward, and stress gates before any live discussion. |
| 2026-05-22 | `data/models/20260519_v95_v84_selective_nearmiss_gate` next replay hypothesis gate | rejected/read-only no-new-data gate | `0.98` primary, `0.94` near, no runtime overlay | no new real close after `2026-05-21 20:42:26`; post-target support train/validation/final/live `5/0/4/3` | n/a | live since restart still `-0.00125657` | live still `11.1111%` | n/a | n/a | Research `docs/research/20260522-next-replay-hypothesis/summary.md`; gate artifact `docs/research/20260522-next-replay-hypothesis/no_new_data_gate.json`; reused `docs/research/20260521-post-target-exit-state/summary.md`, `docs/research/20260521-conditional-exit-flow-state/summary.md`, and `docs/research/20260522-post-ci-research-round/summary.md`; external Claude session `524ac560-40e4-4f1f-93bb-f1cf720bbdbe`; `read_only=true`, `live_switch_evidence=false`, `safe_for_live_switch=false`. The latest live state has no open positions and no new closed trade after `币安队长` at `2026-05-21 20:42:26`. The existing conditional-exit feasibility gate still blocks a giveback guard because `post_target_collapse_or_live_mfe_giveback` has train `5`, validation `0`, final `4`, and live `3`, below the validation `>=3` support gate. Recent 200 signal decisions are still mostly `buy_model_reject=91` and `near_threshold_pred_return_below_min=86`, so there is no new clean entry-widening evidence. | Do not rerun deterministic feasibility as a timestamp-only refresh, do not implement a giveback guard, do not change `.env`, thresholds, sizing, runtime overlays, or model artifacts. Wait for new lifecycle/live evidence or a structurally different exit-state validation design before revisiting post-target/giveback protection. |
| 2026-05-22 | `data/models/20260519_v95_v84_selective_nearmiss_gate` post-CI rejected-signal and live-loss forensic refresh | rejected/read-only diagnostic | `0.98` primary, `0.94` near, no runtime overlay | `66` current-day rejected candidates; pooled `898`; live-loss table `16` losses | n/a | live loss-only `-0.00166791`; live total since restart still `-0.00125657` | live `11.1111%` before this no-new-close refresh | n/a | n/a | Research `docs/research/20260522-post-ci-research-round/summary.md`; reports `data/replay_reports/time_to_barrier_probe_20260522_post_ci_current_day_all_candidates.json`, `data/replay_reports/support_action_policy_20260522_post_ci_current_day.json`, and `data/replay_reports/support_action_policy_pool_20260522_post_ci_current_plus_expanded.json`; `read_only=true`, `live_switch_evidence=false`, `safe_for_live_switch=false`. The current-day rejected-signal slice had only `66` per-token candidates from `2010` signal decisions, below the pre-registered `150` support floor. The best eligible current-day rule was `high_prob_low_toxic_overlap` with `7` selected and `4` positives (`57.14%`), but that number is not actionable because support is too thin. Pooled evidence still rejected the target rule: `142` selected, `68` positives, `47.89%` precision, and `decision=missing_flow_feature_parity`. The live-loss forensic table found the dominant loss buckets remain `dead_flow_timeout=7`, `mfe_then_giveback=3`, and `entry_slippage_failure=2`, with no new closed trade after `2026-05-21 20:42:26`. | Do not switch live, do not change `.env`, model artifacts, sizing, thresholds, or runtime overlays. Freeze the rejected-signal flow-support loop for now. Future work should be a fresh replay-integrated test of high-positive-slippage entry protection or a giveback guard for trades that reach `+25%` MFE then close at STOP_LOSS; neither is live evidence until validation/final/walk-forward/stress gates pass. |
| 2026-05-22 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + conditional dead-flow exit replay | rejected/runtime replay | `0.98` primary, `0.94` near, exit grid `min_hold in {90,120,180,240}`, `max_mfe in {0.03,0.05,0.08}` | validation selected `32`; final selected `24` | n/a | validation selected `0.01880075`; final selected `0.01029771` | validation selected `78.1250%`; final selected `58.3333%` | validation selected `-27.4492%`; final selected `-15.9517%` | validation selected `67.1722%`; final selected `-2.3490%` | Research `docs/research/20260522-conditional-dead-flow-exit-replay/summary.md`; report `data/replay_reports/dead_flow_exit_replay_20260522_v95.json`; task `.ccg/tasks/archive/2026-05/live-model-optimization-20260522-conditional-dead-flow-exit-replay`; `live_switch_evidence=false`, `safe_for_live_switch=false`. The replay preserved v95 entries and tested only default-off exit overrides. Best validation cell was `min_hold=120s`, `max_mfe=0.08`, with `3` dead-flow exits and frozen entries, improving net profit from `0.01849380` to `0.01880075` BNB, but it missed the required `0.0005` BNB materiality gate and weakened stress worst profit from `0.01294879` to `0.01284423` BNB. Sealed final had `0` dead-flow exits and no net-profit improvement, while stress worst profit dropped from `0.00239452` to `0.00142257` BNB and stress worst drawdown worsened from `-20.1452%` to `-22.1800%`. | Do not switch live, do not change `.env`, and do not promote hold-plus-MFE dead-flow exit as a live runtime toggle. The useful lesson is that exit-only frozen-entry experiments are feasible, but this simple dead-flow rule is too weak; a future attempt needs richer causal post-entry state or a candidate-level learned/meta gate with strict stress constraints. |
| 2026-05-22 | `data/models/20260519_v95_v84_selective_nearmiss_gate` live trade attribution refresh | rejected/read-only attribution | `0.98` primary, `0.94` near | `18` live closed | n/a | live `-0.00125657` | live `11.1111%` | n/a | n/a | Research `docs/research/20260522-live-trade-attribution-refresh/summary.md`; report `docs/research/20260522-live-trade-attribution-refresh/live_attribution.json`; task `.ccg/tasks/archive/2026-05/live-model-optimization-20260522-live-attribution-refresh`; `read_only=true`, `live_switch_evidence=false`, `safe_for_live_switch=false`. The reproducible raw-file rebuild matched the prior v95 restart attribution: `18` real closed trades, `2` wins / `16` losses, `0` missing lifecycle paths, labels `dead_flow_timeout=7`, `mfe_then_giveback=3`, `entry_slippage_failure=2`, `stop_first_after_entry=1`, `unprofitable_other=3`, `profitable_exit=2`. Near-threshold trades were `8`, mostly `dead_flow_timeout=6` and `unprofitable_other=2`; primary trades carried all `3` MFE givebacks plus both profitable exits. | Do not switch live, do not change `.env`, model artifacts, sizing, or runtime thresholds. This closes the refresh as evidence discipline rather than a new candidate: live attribution is reproducible and still points to conditional dead-flow exit or candidate-level meta-gate as future replay-only hypotheses, but this round produced no causal, replay-equivalent rule safe for live. |
| 2026-05-22 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + active-flow total-buys quick-profit replay | rejected/runtime replay | `0.98` primary, `0.94` near, overlay `prob>=0.985`, `10<=PredReturn<=35`, `total_buys in {6,10,14}` | validation selected `136`; final selected `92` | final selected `267.7367%` | final selected `0.01359916` | final selected `63.0435%` | final selected `-9.3224%` | final selected `22.7501%` | Research `docs/research/20260522-active-flow-quick-profit-replay/summary.md`; report `data/replay_reports/active_flow_quick_profit_replay_20260522_v95.json`; task `.ccg/tasks/live-model-optimization-20260522-active-flow-replay`; `live_switch_evidence=false`, `safe_for_live_switch=false`. The grid was deliberately narrowed after external Claude analysis: only `buy_quick_profit_overlay_min_total_buys` was added as a default-off deployable active-flow proxy, while overlap/reentry filters were deferred because deployable feature extraction defaults missing denominator ratios to `0.0`. Validation headline profit improved from `0.01614948` to `0.02125178` BNB at `min_total_buys=6`, but trades expanded from `32` to `136`, win rate dropped from `81.25%` to `63.2353%`, WF worst return fell from `62.6794%` to `53.3993%`, and stress worst profit fell from `0.01110019` to `0.00887510` BNB. Final confirmation also failed: trades expanded from `24` to `92`, win rate fell from `70.8333%` to `63.0435%`, max DD worsened from `-7.3620%` to `-9.3224%`, WF worst DD worsened from `-14.3771%` to `-18.6450%`, and stress worst profit fell from `0.00431422` to `0.00251853` BNB. | Do not switch live, do not change `.env`, and do not promote `total_buys` as a standalone active-flow rescue proxy. This confirms the broader quick-profit overlay failure shape: headline profit can rise while quality, trade-count discipline, and stress robustness degrade. Next work should fix flow missingness parity before using overlap/reentry filters, or move to a candidate-level learned/meta gate with explicit trade-count and stress constraints. |
| 2026-05-22 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + expanded all-candidate flow support gate | rejected/diagnostic support gate | `0.98` primary, `0.94` near, no runtime overlay | `832` rejected candidates; target rule selected `135` | n/a | n/a | n/a | n/a | n/a | Research `docs/research/20260522-expanded-flow-evidence-gate/summary.md`; reports `data/replay_reports/time_to_barrier_probe_20260522_expanded_flow_since20260521_all_candidates.json`, `data/replay_reports/support_action_policy_pool_20260522_expanded_flow_all_candidates.json`, and `data/replay_reports/support_action_policy_flow_rule_diagnostic_20260522_expanded.json`; task `.ccg/tasks/live-model-optimization-20260522-expanded-flow-evidence`; `live_switch_evidence=false`, `safe_for_live_switch=false`. The all-candidate report removed the old `100` row sample cap and scored `22093` rejected signal decisions into `832` per-token candidates: `156` positives and `676` skips. The pre-registered `high_prob_low_toxic_overlap` target now passes raw count support (`135` selected, `64` positives versus `30/12` minimums) but fails flow parity: `flow_buy_sell_overlap_ratio_60s` finite `607/832`, `flow_recent_seller_reentry_ratio_30s` finite `544/832`. Expanded target precision fell to `47.41%` with `71` selected negatives. A post-hoc active-flow diagnostic found `prob>=0.985`, `age<=60`, `entry_volume_30s>=1.25`, `flow_event_count_30s>=10`, `flow_buy_sell_overlap_ratio_60s<=0.5` selected `38` with `23` positives (`60.53%`), but that is support-only and post-hoc. | Do not implement a runtime overlay, do not switch live, and do not treat undefined buyer-overlap/reentry ratios as clean flow. The useful next direction is a replay-integrated, pre-registered active-flow candidate overlay/meta-filter around young high-probability candidates, with separate validation/final/walk-forward/stress gates and 10% sizing unchanged. |
| 2026-05-22 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + flow-aware quick-profit pooled support gate | rejected/diagnostic support gate | `0.98` primary, `0.94` near, no runtime overlay | `104` pooled rejected candidates; target rule selected `13` | n/a | n/a | n/a | n/a | n/a | Research `docs/research/20260522-flow-quick-profit-overlay/summary.md`; report `data/replay_reports/support_action_policy_pool_20260522_flow.json`; task `.ccg/tasks/live-model-optimization-business-round-20260522`; `live_switch_evidence=false`, `safe_for_live_switch=false`. The pooled support gate combined `data/replay_reports/time_to_barrier_probe_20260521_flow_fields_live.json` and `data/replay_reports/time_to_barrier_probe_20260522_latest_rejects.json`: `104` candidates, `22` oracle positives, `82` negatives. The pre-registered `high_prob_low_toxic_overlap` rule selected `13`, with `9` positives and `4` negatives, precision `69.23%`, below the required `30` selected and `12` positives. Required flow fields were also incomplete: `flow_event_count_30s` finite `103/104`, `flow_buy_sell_overlap_ratio_60s` finite `80/104`, and `flow_recent_seller_reentry_ratio_30s` finite `71/104`. Static negative controls stayed weaker: `high_prob_volume_volatility` precision `46.67%`, `high_prob_positive_pred` precision `33.33%`, and `v95_like_pred_rescue` precision `33.33%`. | Do not implement the runtime/replay overlay in this round. The flow direction is still more informative than static high-score buckets, but it failed the pre-registered expanded-evidence and flow-parity gates. No `.env`, model artifact, live runtime, position sizing, or trading-service change is justified. A future round may revisit only after collecting larger complete-flow rejected-signal evidence. |
| 2026-05-22 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + default-off entry slippage risk veto replay | rejected/runtime replay | `0.98` primary, `0.94` near, veto grid/probes | strict limited grid `32`; loose trigger `5`; small sweep `24-29` | strict same as baseline; loose trigger much lower | strict `0.01614948`; loose trigger `0.00075494`; small sweep best `0.01532000` | strict `81.25%`; loose trigger `80.00%`; small sweep best `82.76%` | strict `-31.7694%`; loose trigger `-27.5019%`; small sweep best `-31.7694%` | strict `62.6794%`; loose trigger `0.0000%`; small sweep best `61.9996%` | Research `docs/research/20260522-entry-slippage-risk-veto/summary.md`; task `.ccg/tasks/live-model-optimization-business-round-20260522`; reports `data/replay_reports/entry_slippage_risk_veto_replay_20260522_v95_limited8.json`, `data/replay_reports/entry_slippage_risk_veto_probe_drawdown0_20260522_v95.json`, `data/replay_reports/entry_slippage_risk_veto_probe_loose_trigger_20260522_v95.json`, and `data/replay_reports/entry_slippage_risk_veto_probe_small_sweep_20260522_v95.json`; `live_switch_evidence=false`, `safe_for_live_switch=false`. The full 64-candidate replay was stopped after about 30 minutes with high CPU and no report, then bounded diagnostics were used. Strict planned thresholds produced `0` veto rejects and no profit improvement; the drawdown-zero probe still produced `0` rejects. A loose trigger proved the branch can reject (`127` rejects), but collapsed to only `5` trades and `0.00075494` BNB profit. The targeted small sweep produced real rejects (`12-40`) but all candidates cut profit and/or worsened drawdown versus baseline. | Do not switch live, do not change `.env`, and do not promote this static entry-slippage veto. The reusable work is the default-off replay instrumentation and report script; the research lesson is that simple price-extension/drawdown/recent-jump thresholds do not isolate profitable fill-risk cases under v95. Next work should use richer causal flow/path-state features or learned candidate-level filtering, not another static slippage threshold grid. |
| 2026-05-21 | v98 optional flow-feature token-holdout probes: `data/models/20260521_v98probe_noflow_recent6_tokenholdout` and `data/models/20260521_v98probe_flow_recent6_tokenholdout` | rejected/training probe | `1.0` selected in both probes | no-flow `1`; flow `0` | no-flow `-4.1162%`; flow `0.0000%` | no-flow `-0.00024683`; flow `0.00000000` | no-flow `0.0000%`; flow n/a | no-flow `-4.1162%`; flow `0.0000%` | n/a | Research `docs/research/20260521-post-pump-decay-veto/summary.md`; reports `data/models/20260521_v98probe_noflow_recent6_tokenholdout/noflow_training_report.json` and `data/models/20260521_v98probe_flow_recent6_tokenholdout/flow_training_report.json`. The full all-data no-cache v98 run was stopped after the process stayed in Python sample/feature extraction for over an hour with no artifacts, so this falsification used a recent-six-file token-holdout probe. To avoid train/eval token leakage, eval tokens from the two 2026-05-21 lifecycle files were excluded from the four-file train set. The optional flow feature implementation preserved old feature semantics by default and only enables the additional sell-pressure/imbalance fields when the model schema requires them. The no-flow control still lost money with one trade, while the flow variant selected no trades; this is capital-protective but not a profit improvement over v95. | Do not switch live. Do track the report JSON artifacts, but do not commit rejected probe model binaries as deployable artifacts. The useful lesson is structural: sell-pressure/imbalance should be tested next as a conditional veto or penalty around existing v95 candidates, not as a broad added feature set that calibrates to `1.0` and stops trading. |
| 2026-05-21 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + flow-enhanced path-state meta gate replay | rejected/runtime replay | path-state score `0.35-0.99` | `32` final selected | `390.4589%` final selected | `0.01983259` final selected | `81.25%` | `-4.6523%` | `16.6159%` final selected | Research `docs/research/20260521-flow-meta-replay-gate/summary.md`; plan `docs/superpowers/plans/2026-05-21-flow-enhanced-path-state-meta-gate.md`; replay report `data/replay_reports/path_state_meta_gate_replay_20260521_flow_enhanced_v95.json`; `live_switch_evidence=false`, `decision=reject`. Live-first evidence included `XYZ` as a clean missed runner, `CTW`/`TEST` as high-score stop-first counterexamples, `Fren` as a noisy-flow runner, and a new real v95 loss `🆙`: `prob=0.9816`, `PredReturn=44.33`, favorable `-5.55%` entry slippage, but peak-to-signal drawdown `-63.35%`, pre-signal 10s flow `0` BNB buy vs `4.5169` BNB sell, post-signal MFE `-7.42%`, and `TIME_EXIT` loss `-0.00002809` BNB. The implementation copied causal signal-time flow/toxicity fields into `path_state_meta_probe` and kept 10% sizing. Validation rejected the direction: thresholds `0.35-0.90` were exact no-ops (`27` trades, `0` rejects, `0.00946925` BNB), while `0.95-0.99` were no-trade (`105` rejects, `0` entries). Final selected `0.35` was also an exact no-op versus final baseline (`32` trades, `0` rejects, `0.01983259` BNB), so `final_confirmation.passes_acceptance_gate=false`. | Do not switch live. The broad path-state meta gate still has no usable middle band even with added flow/toxicity fields. Next work should be a narrower post-pump decay / sell-only pre-signal veto using causal short-window sell volume, normalized sell pressure, and peak-to-signal drawdown, not another global threshold change or broad learned path-state gate. |
| 2026-05-21 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + ultra-short runner quick-profit replay | rejected/runtime replay | `0.98` primary, `0.94` near, overlay `prob>=0.985`, `PredReturn>=10`, `volume_30s>=1.25/1.35`, `age<=5s`, `TP=25%`, `max_hold=15s` | `111` final selected | `534.8448%` final selected | `0.02716639` final selected | `68.4685%` | `-5.4462%` | `41.1877%` final selected | Research `docs/research/20260521-ultra-short-runner-entry-exit/summary.md`; plan `docs/superpowers/plans/2026-05-21-ultrashort-runner-replay.md`; report `data/replay_reports/ultrashort_runner_replay_20260521_v95.json`; `live_switch_evidence=false`. Live trigger was `卡西法`, a high-probability, young, positive-PredReturn near-miss that still had delayed-entry MFE for a very short scalp, while nearby rejects mostly collapsed after realistic entry delay. Wider `288`, `96`, and `32` grids were stopped as too slow for the live loop; the final 4-candidate test preloaded replay samples and kept 10% sizing. Validation raw profit improved from `0.00946925` to `0.01479169` BNB, but the selected candidate expanded validation trades from `27` to `110`, worsened win rate to `57.2727%`, worsened max DD to `-32.0832%`, and cut stress-worst profit to `0.00485778` BNB. Sealed final also improved headline profit from `0.01983259` to `0.02716639` BNB, but expanded trades from `32` to `111`, dropped win rate from `81.25%` to `68.4685%`, worsened max DD/WF DD, and reduced stress-worst profit/return (`0.00838545` BNB / `165.0905%` vs baseline `0.00863495` BNB / `170.0026%`). During the run, a new live near-rescue trade `人间半夏小得盈满` opened at `prob=0.9782`, `PredReturn=48.08`, then TIME_EXIT lost `-0.00005712` BNB; post-signal path had only `+3.16%` MFE from signal and negative MFE from entry, with 15s sell pressure far above buy pressure. | Do not switch live. The ultra-short opportunity exists, but this deterministic overlay over-expands low-quality entries and fails win-rate, trade-count, drawdown, walk-forward drawdown, and stress gates. Next work should target a near-rescue decay/sell-pressure veto or learned candidate-level filter that rejects cases like `人间半夏小得盈满`, not another broader quick-profit entry overlay. |
| 2026-05-21 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + flow-toxicity signal-time support probe | supports follow-up/read-only probe | `0.98` primary, `0.94` near, causal flow fields only | `66` live rejected candidates | n/a | n/a | n/a | n/a | n/a | Research `docs/research/20260521-flow-toxicity-near-rescue-gate/summary.md`; reports `data/replay_reports/time_to_barrier_probe_20260521_flow_fields_live.json` and `data/replay_reports/support_action_policy_probe_20260521_flow_fields_live.json`; `live_switch_evidence=false`. The probe added causal signal-time lifecycle flow fields to rejected-signal barrier candidates: buy/sell volumes, sell pressure, signed imbalance, event counts, buy/sell overlap, seller re-entry, and buyer churn over `10/30/60s` windows, using only events at or before the signal timestamp. Latest post-close live window had `1545` rejected decisions, `66` per-token candidates, `14` quick-take-profit labels, and `52` skips. `XYZ` remained the cleanest missed runner, but `v95_like_pred_rescue` was only `1/2` positive after `CTW` joined the same support bucket; `high_prob_low_toxic_overlap` selected `9` with `6` positives and `3` negatives (`spacexcoin`, `TEST`, `CTW`); `Fren` was a real runner but had high seller overlap/pressure, so a simple low-toxicity rule would skip it. | Do not switch live. The field pipeline works, but a static low-sell-pressure / low-overlap gate is not enough: it still admits stop-first cases like `TEST` and can miss real runners like `Fren`. Next experiment should use these causal flow fields inside a replay-integrated learned/support-constrained candidate meta-gate, with validation/final/walk-forward/stress gates and 10% sizing unchanged. |
| 2026-05-21 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + dead-bounce entry veto replay | rejected/runtime replay | `0.98` primary, `0.94` near, dead-bounce veto grid | `27` validation raw-best | `181.1033%` validation raw-best | `0.00702364` validation raw-best | `74.0741%` | `-28.9395%` | `18.6188%` validation raw-best | Research `docs/research/20260521-dead-bounce-entry-veto/summary.md`; plan `docs/superpowers/plans/2026-05-21-dead-bounce-veto-replay.md`; report `data/replay_reports/dead_bounce_veto_replay_20260521_v95.json`; `live_switch_evidence=false`, `safe_for_live_switch=false`. Live trigger was the real `domybest` v95 primary buy: high `prob=0.9849` and `PredReturn=56.75`, but about `+11.54%` entry slippage, roughly `-70.8%` pre-signal peak drawdown, heavy immediate sell flow, and no post-entry recovery before `PPO_SELL100`. The replay-only implementation added disabled-by-default `buy_dead_bounce_veto_*` overrides using causal current-sample fields and kept 10% sizing. The bounded 16-candidate validation grid was falsified before sealed final: every candidate matched the baseline exactly with `dead_bounce_veto_reject_count=0`, `27` trades, `0.00702364` BNB profit, and harsh-stress worst `138.9970%` return. | Do not switch live and do not add this static dead-bounce veto to runtime. The live failure shape is real, but current replay features/thresholds did not identify any validation v95 entries, so this hard rule has no validated edge. Next attempt should either add/log richer causal short-window flow features for replay/live parity or move back to learned candidate-level / conditional-exit labels, not another static threshold-only veto. |
| 2026-05-21 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + support-constrained action policy probe | supports follow-up/read-only probe | `0.98` primary, support rules only | `74` rejected path candidates | n/a | n/a | n/a | n/a | n/a | Research `docs/research/20260521-conservative-action-policy-from-oracle-labels/summary.md`; plan `docs/superpowers/plans/2026-05-21-support-constrained-action-policy-probe.md`; report `data/replay_reports/support_action_policy_probe_20260521_post_cmc_live.json`; `live_switch_evidence=false`, `safe_for_live_switch=false`. Live-first support scan over the post-CMC rejected-signal report found `13` positive oracle labels and `61` skips. The hard-abstain bucket `prob<0.94` selected `35` candidates with `0` positives, reinforcing the earlier rejection of global threshold lowering while avoiding overlap with the v95 near-rescue boundary. The best tiny support buckets were `high_prob_positive_pred` (`prob>=0.985`, `PredReturn>=5`, `5/5` positive) and `young_high_prob_positive_pred` (`prob>=0.985`, `PredReturn>=5`, `age<=60`, `4/4` positive). The broader `high_prob_volume_volatility` bucket selected `8` with `6` positives but included dangerous stop-first counterexamples `ATOS` and `MEMES`. | Do not switch live. This report uses decision-time features for rules, but its labels are still ex-post path outcomes and support is small. Promote only the narrow `prob>=0.985` + positive-PredReturn rescue shape to a replay-integrated action-policy experiment; reject global threshold lowering, broad volume/volatility rescue, and treating `conditional_slow_hold` as deployable with current support. Any replay candidate must beat current best v95 on validation, sealed final, walk-forward, stress, drawdown, trade count discipline, and 10% sizing before deployment. |
| 2026-05-21 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + counterfactual action probe | supports follow-up/read-only probe | `0.98` primary, diagnostic action layer only | `40` rejected path candidates + `31` post-target candidates | n/a | n/a | n/a | n/a | n/a | Research `docs/research/20260521-live-counterfactual-policy-selection/summary.md`; plan `docs/superpowers/plans/2026-05-21-counterfactual-action-probe.md`; report `data/replay_reports/counterfactual_action_probe_20260521_post_cmc_live.json`; `live_switch_evidence=false`, `safe_for_live_switch=false`. Live evidence first: after the real `CMC` STOP_LOSS, the bot stayed flat with `770` rejected signal decisions. A fresh time-to-barrier scan found `fast_profit=5`, `fast_profit_then_collapse=1`, `stop_first=6`, and `flat_timeout=28`; only `Arnold` passed the strict probe rescue gate (`prob=0.9878977`, `PredReturn=32.1704`, MFE `+334.6%`, MAE `-9.7%`, `+25` in `56.9s`). The same window still contained dangerous high-MFE fakeouts such as `MEMES`, which hit `-18` before later running, so this is not evidence for global threshold lowering. Combining the final post-target probe added `25` continue-hold, `4` post-target-lock, and `2` monitor states, including final/live-like `CMC`. | Do not switch live. This is an oracle action taxonomy and counterfactual evidence report only; labels use ex-post path outcomes and are not a causal deployable policy. Taxonomy: `skip`, `rescue_quick_tp`, `conditional_slow_hold`, `post_target_lock`, `continue_hold`, and `monitor_after_target`. It supports a replay-integrated action-policy experiment that must keep 10% sizing and beat current best v95 on validation, sealed final, walk-forward, stress, drawdown, and trade discipline before any live change. |
| 2026-05-21 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + post-target exit-state probe | supports follow-up/read-only probe | `0.98` primary, diagnostic only | `27` validation / `31` final scored | n/a | n/a | n/a | n/a | n/a | Research `docs/research/20260521-post-target-exit-state/summary.md` and `docs/research/20260521-rare-exit-validation/summary.md`; reports `data/replay_reports/post_target_exit_state_probe_20260521_v95_train.json`, `data/replay_reports/post_target_exit_state_probe_20260521_v95_validation.json`, and `data/replay_reports/post_target_exit_state_probe_20260521_v95_final.json`; `live_switch_evidence=false`. Live trigger was the real CMC trade: a high-confidence v95 primary buy reached delayed profit (`+25/+35`) but never `+60`, then collapsed to `STOP_LOSS`. The probe kept 10% sizing and max `8` positions and only classified already accepted v95 replay trades after target hit. Chunked train diagnostic is not equivalent to full strict train replay because replay state resets per lifecycle file; it is rare-state discovery only. It skipped the unsafe `1.85GB` legacy lifecycle file and one no-episode file, then scored `59` trades from `218848` samples: `49` target-hit candidates, `42` continuations, `2` unresolved states, `10` target-not-hit states, and `5` post-target collapses (`模因`, `SUPERCYCLE`, `MALO`, `Binance Movie`, `COB`). Validation had `27` scored trades, `23` target-hit candidates, `22` continuation states, `1` unresolved post-target state, and `0` post-target collapses. Final had `31` scored trades, all target-hit, with `25` continuations, `2` unresolved states, and `4` post-target collapses (`SPACEASTEROID`, `BinancialFreedom`, `BNB SZN`, and live-like `CMC`). | Do not switch live. The failure shape is real in final/live evidence, but validation has no collapse examples, so no rule can be selected without overfitting. Use this only as evidence for a future replay-integrated conditional exit model that activates after `+25/+35` and must beat current best v95 on validation, sealed final, walk-forward, and stress before deployment. |
| 2026-05-21 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + delayed profit-lock replay | rejected/runtime replay | `0.98` primary, delayed TP grid only | `27` validation raw-best | `122.3568%` validation raw-best | `0.00484201` validation raw-best | `85.1852%` | `-4.7297%` | `34.4239%` validation raw-best | Research `docs/research/20260521-delayed-profit-lock-event-exit/summary.md`; plan `docs/superpowers/plans/2026-05-21-delayed-profit-lock-replay.md`; report `data/replay_reports/delayed_profit_lock_replay_20260521_v95.json`. Live trigger was the real v95 primary `CMC` trade: `prob=0.9885`, `PredReturn=43.32`, about `5.22%` entry slippage, then a `STOP_LOSS` after `491.6s`. Reconstructed lifecycle showed it hit `+25%` about `225s` after entry, `+35%` about `459s` after entry, peaked near `+37%`, then collapsed below the stop within seconds. This made delayed full-position TP a distinct test from the previously rejected `30-120s` fast profit-lock. The bounded grid kept 10% sizing, max `8` positions, no fixed stake, no new entries, and swept targets `25/35/45/60%` with windows `180/240/360/480s`. Validation rejected the direction before sealed final: baseline was `27` trades, `181.5531%` return, `0.00718457` BNB, `74.0741%` win rate, `-28.8858%` max DD, and `139.4841%` stress-worst return. Raw-best delayed TP used `60%` with `180s` or `240s`, produced `15` profit-lock exits, improved win rate/drawdown/WF, but cut net profit to `0.00484201` BNB and stress-worst return to `69.9031%`; strict gates failed on net profit and stress. | Do not switch live and do not add blanket delayed profit-lock runtime. CMC was a real missed-exit example, but a generic delayed TP over accepted v95 positions over-cuts durable runners. Next work should model conditional exits that only lock profit when post-target path/flow shows decay risk, or train/score an exit state after target hit, rather than fixed full-position TP windows. |
| 2026-05-21 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + conditional low-volume rescue / pump-risk veto replay | rejected/runtime replay | `0.98` primary, near rescue disabled in candidates | `31` final selected | lower than baseline | `0.01551477` final selected | `77.4194%` | `-4.9783%` | `47.8955%` final selected | Research `docs/research/20260521-conditional-volume-pump-risk/summary.md`; plan `docs/superpowers/plans/2026-05-21-conditional-volume-pump-risk-replay.md`; report `data/replay_reports/conditional_volume_pump_risk_replay_20260521_v95.json`. Live trigger was v95 losing real money through near rescue, pump/slippage failures (`FENGSHUI`, `挠头`), and mixed low-volume missed runners (`EX677`, `FourPass`, `GNGN`). During this run a new primary signal, `CMC/COINMARKETCAT`, bought at `prob=0.9885`, `PredReturn=43.32`, `5.22%` entry slippage, then closed by `STOP_LOSS` for `-0.00022816` BNB after about `491.6s`, adding the `primary_high_prob_slow_decay` failure shape. The bounded grid kept 10% sizing, disabled near-threshold rescue, enabled only low-volume primary rescue, and applied the existing late-pump veto with low volume floor `0.0`. Validation rejected the idea before final: baseline was `27` trades, `0.00768698` BNB, `74.0741%` win rate, `-28.7314%` max DD, and `140.8140%` stress-worst return; selected validation candidate fell to `26` trades, `0.00608547` BNB, `73.0769%` win rate, had `1` low-volume entry but `0` late-pump veto rejections, and worsened stress. Final confirmation also failed versus its baseline: `0.01551477` BNB vs `0.01595669` BNB, `77.4194%` vs `80.0000%` win rate, worse max DD, worse walk-forward DD, and worse stress (`142.3502%` vs `166.4163%` stress-worst return). | Do not switch live. Deterministically disabling v95 near rescue and adding low-volume rescue plus broad pump veto removes some fragile behavior but also removes too much edge and does not robustly solve the new primary high-probability slow-decay loss. Next work should target conditional early exit / decay detection or labels that penalize candidates whose post-signal path fails to make early MFE before timeout, rather than another static volume or pump threshold grid. |
| 2026-05-20 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + path-state meta gate replay | rejected/runtime replay | `0.98` primary, `0.94` near, path-state score sweep `0.35/0.50/0.65/0.75/0.85/0.90/0.95/0.98/0.99` | `46` final selected | `503.2820%` final selected | `0.02556322` final selected | `78.2609%` | `-8.1825%` | `119.7469%` final selected | Research `docs/research/20260520-late-pump-entry-veto/summary.md`; plan `docs/superpowers/plans/2026-05-20-path-state-meta-gate-replay.md`; report `data/replay_reports/path_state_meta_gate_replay_20260520_v95.json`. The replay trained a causal path-state meta-label model on train split only, scored validation/final episodes, and required candidates to both improve profit and actually reject at least one baseline signal. The final implementation used barrier-priority labels (`target_before_stop` positive, `stop_before_target` negative, risk-adjusted fallback only when no barrier field exists), prior same-token buy-probability and entry-score deltas, terminal-sample exclusion for non-enterable rows, pinned validation/final sample snapshots, required episode-metadata score-map validation, JSON numeric-key normalization, stress replay score-map propagation, preloaded eval-sample excluded-token filtering, and compact score-map report summaries. Validation falsified the shape: score thresholds `0.35` through `0.90` accepted all baseline trades (`26` entries, `0` rejects, identical `0.00683256` BNB profit), while `0.95` through `0.99` rejected all `117` gated signals and produced `0` trades. Final selected the raw no-op threshold and matched baseline exactly (`46` trades, `0.02556322` BNB, stress worst `265.8399%` return / `0.01350282` BNB). Related focused tests pass. | Do not switch live. This learned path-state score has no usable middle band: it is either a no-op or a no-trade gate. The live late-pump/slippage problem still stands, but the next attempt should use a more targeted deterministic/learned feature around pump extension, drawdown-from-peak, and entry slippage risk, or a conditional exit model, rather than this broad path-state classifier. |
| 2026-05-20 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + shadow PredReturn-disagreement meta-gate replay | rejected/runtime replay | `0.98` primary, `0.94` near, shadow `prob>=0.988`, `entry_score<=10`, `volume_30s>=2.0`, `price_volatility>=0.25`, `age<=60s`, `shadow_score>=0.50` | `45` final selected | `511.4749%` final selected | `0.02597936` final selected | `80.0000%` | `-8.1825%` | `119.7469%` final selected | Plan `docs/superpowers/plans/2026-05-20-shadow-meta-gate-replay.md`; report `data/replay_reports/shadow_meta_gate_replay_20260520_v95.json`; follows research `docs/research/20260520-highprob-predreturn-disagreement/summary.md`. The replay integrated the positive v97 shadow-ranker probe as a default-off candidate-level meta gate over high-probability/low-PredReturn rejects with 10% sizing. Validation selected candidate `21`: it added only `1` shadow entry and improved validation profit from `0.00683256` to `0.00692253` BNB while keeping win rate, drawdown, walk-forward, and stress gates non-worse. The raw best candidate `12` reached `0.00824164` BNB but failed win-rate, drawdown, and stress-drawdown gates after adding `50` shadow entries. Sealed final confirmation failed: candidate `21` produced `0` shadow entries, exactly matched the v95 final baseline (`45` trades, `0.02597936` BNB, `80.0%` win rate, `-8.1825%` max DD), and failed both the required profit-improvement and shadow-entry gates. | Do not switch live. The learned shadow gate is too sparse under sealed final data, while looser settings over-expand and weaken risk. Keep the evidence that the disagreement bucket is mixed, but the next round should use path-state features or conditional exit labels rather than another static high-probability/low-PredReturn entry rescue. |
| 2026-05-20 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + shadow PredReturn-disagreement candidate ranker probe | supports follow-up/replay probe | `0.98` primary, shadow `prob>=0.988`, `score<=10`, `volume_30s>=2.0`, `price_volatility>=0.20`, `age<=60s` | probe only | n/a | n/a | n/a | n/a | n/a | Research `docs/research/20260520-highprob-predreturn-disagreement/summary.md`; report `data/replay_reports/v97_shadow_disagreement_ranker_probe_20260520.json`. The latest live near-miss window after `2026-05-19 19:16:00` had `224` rejected per-token candidates: `quick_take_profit=43`, `conditional_slow_hold=3`, and `skip=178`, with `SZN` as the cleanest missed runner but `交易鸭/cwh` proving the bucket is mixed. The default-off shadow candidate ranker adds high primary-probability PredReturn rejects only to an offline candidate universe, not to live runtime. It improved validation top relevance from `122` to `133`, clean-runner selections from `30` to `32`, and collapses from `28` to `24`; final improved top relevance from `51` to `53`, clean runners from `8` to `9`, and collapses from `38` to `37`. | Do not switch live. This is positive evidence for a replay-integrated candidate-level meta gate, but not executable live evidence. Next step is a replay-only meta gate that must beat v95 on final profit, win rate, drawdown, walk-forward, stress, and trade-count discipline before any live change. |
| 2026-05-20 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + flow-activation/dead-flow replay gate | rejected/runtime replay | `0.98` primary, `0.94` near | `26` final selected | lower than baseline | `0.01560010` final selected | `73.0769%` | `-10.0902%` | `68.0004%` final selected | Report `data/replay_reports/flow_activation_replay_20260520_v95.json`; research `docs/research/20260520-flow-activation-gate/summary.md`; plan `docs/superpowers/plans/2026-05-20-flow-activation-replay-gate.md`. A focused 16-candidate batch tested a replay-only bounded flow activation gate and dead-flow exit at 10% sizing after the full grid proved too slow for the live loop. Best validation candidate used `prob>=0.989`, `PredReturn>=40`, `age<=60s`, `lookback=30s`, `volume_ramp_ratio>=1.8`, `volume_delta>=0.8`, `PredReturn_delta>=5`, `price_volatility_delta>=0.04`, `current_volume_30s>=1.5`, and dead-flow exit after `180s` if MFE stayed `<=5%`. Validation baseline was `26` trades, `0.00685365` BNB profit, `69.2308%` win rate, `-20.3901%` max DD, and `16.2821%` WF worst return; the candidate improved win rate and drawdown but cut profit to `0.00524705` BNB, trade count to `11`, WF worst to `15.2618%`, and stress profit/return slightly below baseline. Final confirmation failed more clearly: v95 baseline was `45` trades, `0.02605458` BNB profit, `80.0000%` win rate, `-8.1803%` max DD, `119.7754%` WF worst, and harsh-stress worst `274.2431%`; selected flow gate fell to `26` trades, `0.01560010` BNB, `73.0769%` win rate, `-10.0902%` max DD, `68.0004%` WF worst, and harsh-stress worst `152.2424%`. | Do not switch live. Simple bounded flow activation mostly acts as an over-aggressive trade-count reducer: it removes some weak trades but also removes too much baseline edge and fails profit, trade-count, win-rate, walk-forward, and stress gates. Keep the path-state evidence, but next work should use learned candidate-level meta-labeling or conditional exit policy with live path features rather than a hard volume-ramp veto. |
| 2026-05-19 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + primary-score scalp quick-profit overlay | rejected/runtime replay | `0.98` primary, `0.94` near | `52` final selected | `447.3709%` final selected | `0.02272332` final selected | `76.9231%` | `-8.6427%` | `78.4564%` final selected | Report `data/replay_reports/primary_score_scalp_replay_20260519_v95.json`; research `docs/research/20260519-short-horizon-scalp-meta-label/summary.md`; implementation is replay-only and keeps 10% sizing. Validation selected candidate `7`: `min_prob=0.988`, PredReturn `[25,35]`, `volume_30s>=2.0`, `price_volatility>=0.10`, `age<=60s`, `take_profit=35%`, `max_hold=120s`, improving validation from `0.00683256` to `0.00893800` BNB with `9` overlay entries. Sealed final failed: v95 baseline was `45` trades, `511.4749%` return, `0.02597936` BNB profit, `80.0000%` win rate, `-8.1825%` max DD, `119.7469%` WF worst return, and `274.1801%` harsh-stress worst return; selected overlay fell to `447.3709%`, `0.02272332` BNB, `76.9231%` win rate, `-8.6427%` max DD, `78.4564%` WF worst return, and `209.7333%` harsh-stress worst return. | Do not add the quick-profit overlay to live. The validation short-scalp pocket is real, but it does not generalize to final and weakens profit, win rate, drawdown, walk-forward, and stress. Keep the replay-only age-safety fix and evidence, but next work should learn a candidate-level meta-label or conditional exit from live path state rather than deploying a simple scalp overlay. |
| 2026-05-17 | `data/models/20260517_v75b_profit_path_hold60_screen` | rejected | `0.956` | `73` | `200.6989%` | `0.01413563` | `61.6438%` | `-14.9665%` | `15.9330%` | Final harsh stress stayed positive around `21%`, but validation harsh friction was `-0.0816%` and validation harsh execution was `-17.6282%`. | Do not switch live. Profit-path hold60 reduced drawdown and raised win rate slightly, but gave up too much return versus v67 and validation stress was unstable. |
| 2026-05-17 | `data/models/20260517_v76_quality30_v67exit_screen` | rejected | `0.9715` | `74` | `217.3068%` | `0.01530535` | `60.8108%` | `-11.8992%` | `18.9700%` | Final harsh friction was `-0.1385%` and harsh execution was `-11.3059%`; validation walk-forward worst was `-2.4786%`. | Do not switch live. Raising the delay-robust target to `30%` with v67-style exits improved drawdown but cut too many profitable trades and failed stress/walk-forward gates versus v67. |
| 2026-05-17 | `data/models/20260517_v77_quality25_v67exit_screen` | rejected | `0.9705575601281782` | `60` | `220.4042%` | `0.01552351` | `68.3333%` | `-8.4673%` | `17.4734%` | Final harsh friction was `28.3951%` on evaluation, but validation harsh execution was only `7.1821%` and validation walk-forward worst was `4.5108%`, still far below v67's return and robustness. | Do not switch live. Lowering the quality target from 30 to 25 recovered some return and win rate, but the model remains too weak in walk-forward and harsh-stress relative to v67. |
| 2026-05-17 | `data/models/20260517_v78_avgpath10_v67exit_screen` | rejected | `0.956` | `17` | `22.3478%` | `0.00157400` | `70.5882%` | `-11.1878%` | `-5.7100%` | Final harsh friction was `-1.8270%` and harsh execution was `-4.2385%`; validation harsh friction was `-6.6158%` with validation walk-forward worst `-6.2537%`. | Do not switch live. Training directly on average path return selected too few trades and failed walk-forward/stress gates, so average-path labels alone are not a viable improvement over v67. |
| 2026-05-18 | `data/models/20260518_v84_v83_method_all_data_prod` + `max_hold=600` | rejected/runtime sweep | `0.98` | `43` | `455.2798%` | `0.02730118` | `76.7442%` | `-7.8221%` | `83.0737%` | Harsh friction stayed positive at `205.3224%` and harsh execution at `178.3553%`, but both regressed versus the 560s candidate. | Do not switch live to this point. It gives up too much win rate and execution-stress robustness versus `max_hold=560`, so it is not the current best controlled-risk baseline. |
| 2026-05-18 | `data/models/20260518_v84_v83_method_all_data_prod` + `max_hold=580` | rejected/runtime sweep | `0.98` | `43` | `473.5995%` | `0.02839974` | `79.0698%` | `-7.8221%` | `80.3173%` | Harsh friction stayed positive at `238.1764%` and harsh execution at `183.8914%`, but both regressed versus the 560s candidate. | Do not switch live to this point. It is close on headline return, but loses to `max_hold=560` on final return, walk-forward, and stress robustness. |
| 2026-05-18 | `data/models/20260518_v84_v83_method_all_data_prod` + `max_hold=480` | rejected/runtime sweep | `0.98` | `43` | `443.7053%` | `0.02660711` | `81.3953%` | `-7.8221%` | `73.4064%` | Harsh friction stayed positive at `250.4936%` and harsh execution at `227.7969%`, but both fell below the 560s candidate. | Do not switch live to this point. It improved over 420s on headline return, but the 560s sweep is stronger on return and walk-forward with similarly bounded drawdown. |
| 2026-05-18 | `data/models/20260518_v84_v83_method_all_data_prod` + `min_policy_hold=60` / `max_hold=420` | rejected/runtime sweep | `0.98` | `43` | `418.6682%` | `0.02510574` | `79.0698%` | `-7.8221%` | `69.7983%` | Harsh friction stayed positive at `254.9553%` and harsh execution at `211.5701%`, but both trail the accepted 45s hold point. | Do not switch live to this point. The v86b hold60 improvement does not transfer to v84: return and win rate fall versus `min_policy_hold=45`, and WF worst drawdown worsens to `-18.0656%`. |
| 2026-05-18 | `data/models/20260518_v86b_delayrobust40_420_probe` + `min_policy_hold=60` | rejected/offline probe | `0.9848365` | `65` | `284.5837%` | `0.01656056` | `60.0000%` | `-17.4749%` | `34.0697%` | Harsh friction stayed positive at `154.6888%` and harsh execution at `128.2034%`. A `min_entry_score=65` variant improved win rate to `82.1429%` and max DD to `-6.6854%`, but reduced return to `232.2611%`; `min_policy_hold=20` fell to `222.3704%` return and `-20.5717%` DD. | Do not switch live. Delay-robust labels plus profit-path BC improved over the live v67 baseline and the v86b default 45s hold, but still trails the accepted v84 420s candidate on return, profit, win rate, drawdown, walk-forward, and harsh stress. Keep the evidence for the next entry/exit design, not as the best model. |
| 2026-05-20 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + replay-only `profit_lock_take_profit_pct=0.6`, `profit_lock_max_hold_seconds=120` | rejected/runtime replay | `0.98` primary, `0.94` near | `26` validation raw-best | `136.1278%` validation raw-best | `0.00691434` validation raw-best | `84.6154%` validation raw-best | `-6.4370%` validation raw-best | `27.1610%` validation raw-best | Report `data/replay_reports/fast_profit_lock_replay_20260520_v95.json`; plan `docs/superpowers/plans/2026-05-20-fast-profit-lock-replay.md`. Validation improved slightly from the v95 baseline (`26` trades, `0.00683256` BNB, `69.2308%` win rate, `-20.3936%` max DD, `16.2752%` WF worst return) to the raw-best replay candidate (`26` trades, `0.00691434` BNB, `84.6154%` win rate, `-6.4370%` max DD, `27.1610%` WF worst return, `16` profit-lock exits). However, validation stress regressed: stress worst return/profit fell from `58.6490%` / `0.00297896` BNB to `57.4628%` / `0.00291871` BNB. The strict runner therefore selected no candidate and skipped final confirmation. | Do not switch live. This replay-only profit-lock is not live-switch evidence because no validation candidate passed the full gate. Keep it only as evidence that simple fast profit-lock exits can over-cut durable edge; next work needs conditional/path-aware exit modeling rather than a blanket take-profit. |
| 2026-05-18 | `data/models/20260518_v87b_entryv_delayrobust_targethit60_fast20_probe` | rejected/offline probe | `0.99` | `0` | `0.0000%` | `0.00000000` | `0.0000%` | `0.0000%` | `0.0000%` | Final strict replay had `27` entry signals, but the delay-robust entry-value gate rejected all `27`; stress replay also had `0` trades in every scenario. Validation only produced `2` trades and `3.6109%` return. | Do not switch live. Training entry value directly on the strict delay-robust return target made the second-stage gate too conservative; buy calibration also predicted `0` positives on `41,017` calibration samples. The next experiment should use a softer runner/collapse meta-label or ranking objective rather than worst-case delay-robust regression as the live gate. |
| 2026-05-18 | `data/models/20260518_v88_avgdelay_entryv_targethit60_fast20_probe` | rejected/offline probe | `0.99` | `6` | `-4.4569%` | `-0.00025936` | `33.3333%` | `-10.7721%` | `-3.3059%` | Base final replay failed; stress had only `1` filled trade per scenario. A threshold sweep at `0.95` and `0.96` produced `168` trades and `252.7598%` return, but max drawdown worsened to `-21.5546%` and WF worst return stayed only `27.0868%`. | Do not switch live. Softening the entry-value target from worst-delay return to average-delay return recovers trade count, but still loses badly to v84's `476.4288%` return, `79.0698%` win rate, `-7.8221%` max DD, and `82.8426%` WF worst return. The next iteration should target a learned runner/collapse meta-label on primary candidates rather than another global threshold relaxation. |
| 2026-05-18 | `data/models/20260518_v89_targethit80_runner_probe_fast20` | rejected/offline probe | `0.985` | `115` | `198.5024%` | `0.01155130` | `50.4348%` | `-25.8014%` | `20.3683%` | Validation reached `87` trades and `148.0637%` return, but final strict replay still underperformed v84 and stress collapsed badly (`-55.9471%`, `-57.4018%`, `-51.2725%` in stress scenarios). | Do not switch live. Raising the target-hit barrier to 80% shrinks the signal set but still fails the strict live-sized comparison: final return and win rate remain below v84, drawdown is far worse, and stress replay is strongly negative. Use this as evidence that the next step needs a dedicated runner/collapse gate, not a stricter global buy target. |
| 2026-05-18 | `data/models/20260518_v91_runnerprob_entryv_fast_probe` | rejected/offline probe | `0.98` | `1440` | `303.1140%` | `0.01684324` | `39.0972%` | `-56.2370%` | `-99.6594%` | Default training evaluation was too sparse (`5` trades, `2.6845%` return, WF worst `-4.9910%`). Lowering the runner-probability entry gate did not fix it: `0.98` with no gate produced `1473` trades, `280.5003%` return, `-61.1327%` max DD, and `-99.7583%` WF worst; `0.98` with `min_entry_score=0.1` still produced `1440` trades and WF near wipeout. Raising the threshold to `0.99` produced `0` trades. | Do not switch live. Training `entry_value_model` directly on the binary `live_target_hit_before_stop` runner label did not create a deployable middle ground: it alternates between too many weak entries and no entries. The next iteration should keep the strong v84 entry model and move the runner/collapse logic into either candidate-level meta-labeling with class-balanced calibration or a conditional exit policy, not a raw binary entry-value gate. |
| 2026-05-18 | `data/models/20260518_v93_tokenbalanced_constrained_probe` | rejected/offline probe | `0.981114` | `85` | `383.0436%` | `0.02296949` | `57.6471%` | `-16.5344%` | `45.9824%` | Final harsh friction was `-2.1399%` and harsh execution was only `2.1517%`; validation was weaker at `134.4651%` return, `-24.3242%` max DD, `6.5623%` WF worst return, and `-3.1294%` harsh friction. | Do not switch live. Token-balanced sampling with high-threshold constraints avoids v91's 1400-trade collapse, but it is still materially behind v84 on return, win rate, drawdown, walk-forward, and stress. Keep the result as evidence for conditional exits or a more focused candidate-level meta-label, not as a live candidate. |
| 2026-05-18 | `data/models/20260518_v84_v83_method_all_data_prod` + `allow_partial_exits=true` | rejected/runtime sweep | `0.98` | `71` | `482.9804%` | `0.02896227` | `71.8309%` | `-7.8221%` | `82.8018%` | Final harsh friction was `269.7413%` and harsh execution `251.9852%`, but validation fell to `125.8871%` return, `-20.1973%` max DD, and `15.7429%` WF worst return. | Do not switch live to this runtime toggle yet. Partial exits improve final headline return and profit, but the validation replay is weaker than the baseline validation profile, so this needs a dedicated conditional-exit model rather than a blanket runtime flag. |
| 2026-05-19 | `data/models/20260518_v84_v83_method_all_data_prod` + near-threshold filter sweep | rejected/runtime sweep | `0.95` | `44` | `498.6497%` | `0.02532793` | `79.5455%` | `-7.9542%` | `107.9857%` | Best validation point from the 48-combo sweep was `buy_threshold=0.95`, `min_entry_score=35`, `min_entry_volume_30s=1.5`, `min_entry_price_volatility=0.1`; validation was only `134.5177%` return with `-20.3936%` max DD and `-31.7694%` WF worst DD. Final improved headline return, but the validation risk did not improve. | Do not switch live. The sweep confirms the live xPBNB-style missed-runner problem exists, but a simple global threshold relaxation is not robust enough. Preserve v84's `0.98` threshold and build a learned second-stage runner/collapse filter or conditional exit instead. |
| 2026-05-19 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + blanket bounded re-entry | rejected/runtime probe | `0.98` primary, `0.94` near | `26` validation | `137.1154%` validation | `0.00822222` validation | `69.2308%` | `-20.1973%` | `16.6648%` validation | Corrected report `data/replay_reports/v95_bounded_reentry_search_20260519_near_aligned.json` replays the v95 near-threshold gate and selects candidate `0`. Candidate `1` (`one_entry_per_token=false`, `max_trades_per_token=2`, 10% sizing) is identical to candidate `0` on validation and does not add trades or improve stress. Final report-only selected baseline returns to v95 manifest metrics: `511.4778%`, `0.03067113` BNB, `44` trades, `near_threshold_entry_count=3`, harsh execution `275.8687%`. | Do not implement blanket bounded re-entry. The broad hook is falsified under the corrected v95 replay. Future re-entry/runner-retention work must be conditional on exit reason, later live signal quality, and path state; the older `v95_bounded_reentry_search_20260519.json` omitted near-gate replay params and should not be used as evidence. |
| 2026-05-19 | `data/models/20260519_v95_v84_selective_nearmiss_gate` + conditional low-volume rescue quick-profit grid | rejected/runtime replay | `0.98` primary, `0.94` near | `63` final selected | final selected lower than baseline | `0.02446624` final selected | `74.6032%` | `-7.9249%` | `125.7844%` final selected | Research `docs/research/20260519-conditional-low-volume-rescue/summary.md`; command `rm -f data/replay_reports/low_volume_rescue_replay_20260519_v95.json && venv/bin/python scripts/run_low_volume_rescue_replay.py --output data/replay_reports/low_volume_rescue_replay_20260519_v95.json`. Report `data/replay_reports/low_volume_rescue_replay_20260519_v95.json` tested 72 replay-only low-volume rescue configs with 10% sizing and no live config changes. Validation selected candidate `1`: `min_prob=0.982`, volume `[0.75,1.5]`, `min_price_volatility=0.05`, `max_age=60`, `take_profit=0.35`, improving validation net profit from `0.00683256` to `0.00836291` with `4` rescue entries and all gates passing. Final confirmation failed: baseline final was `44` trades, `0.02576017` BNB profit, `79.5455%` win rate, `-8.1825%` max DD, and harsh-stress worst `272.4380%`; selected rescue was `63` trades, `0.02446624` BNB profit, `74.6032%` win rate, `-7.9249%` max DD, and harsh-stress worst `270.4929%`, with stress profit/drawdown and trade-count expansion gates also failing. | Do not add a low-volume rescue gate to live. The validation pocket is real, but it does not generalize to final: it over-expands rescued trades and slightly weakens profit, win rate, and stress. Keep the evidence for a narrower candidate-level meta-label or conditional exit model; do not lower `MIN_ENTRY_VOLUME_30S` globally and do not deploy quick-profit rescue as a blanket rule. |
| 2026-05-19 | `data/models/20260519_v94_profitpath_partial_exit_hold45_probe` | rejected/offline probe | `0.941` | `586` | `30.8697%` | `0.00156796` | `26.2799%` | `-36.1253%` | `34.9279%` | Risk tuning fell back because no candidate satisfied constraints. Final stress collapsed (`-70.3856%` mild friction, `-93.2729%` harsh friction, `-92.0207%` harsh execution); validation was also weak with `488` trades, `122.6469%` return, `30.1229%` win rate, `-68.9524%` WF worst return, `-72.6260%` WF worst DD, and negative stress. | Do not switch live. Live-first attribution showed WAGMI was a rapid collapse after a short rebound, while most high-PredReturn rejects also quickly fell; only xPBNB was a clean missed runner. Training a broad profit-path partial-exit policy repeated the weak-signal/high-turnover failure shape instead of learning the narrow runner/collapse separation. |
| 2026-05-17 | `data/models/20260516_v67_v65_thr9715_tr35_12` + `min_entry_score=35` | rejected | `0.9715` | `72` | `185.5333%` | `0.01162366` | `65.2778%` | `-18.6899%` | `19.4009%` | Latest-calibrated validation was strong: `209.1358%` return, `66.2791%` win rate, `58.8336%` WF worst return, harsh friction `52.7281%`, harsh execution `37.0122%`; final harsh stress also stayed positive. | Do not switch live. The entry-value meta gate improves validation/stress and win rate, but final return and WF worst return fall below the accepted hold75 baseline. A stricter `min_entry_score=40` also failed final return at `186.8678%`. |
| 2026-05-17 | `data/models/20260517_v79_barrier25_meta_hold60` | rejected/partial | `0.9532` | `387` | `-99.9504%` | `-0.00703971` | `28.6822%` | `-99.9622%` | `-50.7935%` | Training failed before manifest because `entry_value_target_label_column=live_delay_robust_return_pct` was requested without delay-robust labels, leaving a partial model. Direct replay of the partial buy/PPO artifacts collapsed; threshold sweep only reached `31.3027%` at `0.96` with `-15.2786%` walk-forward worst return, and `0.98+` produced no trades. | Do not switch live. Barrier25 meta-label produced too many weak entries at the learned threshold and could not be salvaged by a simple threshold raise. The training pipeline now rejects missing regression targets instead of silently treating them as zero. |
| 2026-05-18 | `data/models/20260517_v80_tokenbalanced_v67exit_screen` | rejected/partial | `0.745` | `2822` | `1593.8655%` | `0.09985566` | `46.1729%` | `-34.0947%` | `-99.8648%` | Validation had `1871` trades, `1245.6763%` return, `49.7060%` win rate, and `-32.0173%` max DD, but final walk-forward collapsed to `-99.8648%` return and `-99.8666%` max DD. The run was stopped after failure was clear, leaving artifacts inconsistent (`buy_threshold.json=0.5` while manifest evaluation used `0.745`). | Do not switch live. Token-balanced weighting alone created a low-threshold, high-turnover model with huge headline return but unacceptable drawdown, failed final walk-forward, and too many weak trades. |

## Notes

- 2026-05-22 post quick-TP support refresh: read-only reports `data/replay_reports/time_to_barrier_probe_20260522_2314_since_203701.json`, `data/replay_reports/support_action_policy_20260522_2314_since_203701.json`, and `data/replay_reports/low_volume_breakout_probe_20260522_2314_since_203701_prob94.json`; external Claude session `d83e82ad-2e0b-4175-98ea-aef9a15f9aa1`. After the `龙爪` close at `2026-05-22 18:17:47.929280`, there were `0` new paper trades. Since the prior node start at `20:37:01`, the probe scored `2899` signal decisions into `140` per-token candidates: `fast_profit=10`, `fast_profit_then_collapse=16`, `slow_runner=3`, `flat_timeout=81`, `stop_first=29`, and `missing_path=1`, with policy counts `quick_take_profit=26`, `conditional_slow_hold=3`, and `skip=111`. The best eligible support rule was only `10/22` positive (`45.45%` precision), below the already rejected wide-window `v95_like_pred_rescue` precision of `9/14` (`64.29%`). The current-node slice after `23:14:57` had only `2` time-to-barrier candidates, `0` low-volume breakout candidates, and `0` eligible support rules, so those empty local reports are kept under `.ccg` only. Decision: no new scoreboard row because this is thinner evidence for an already rejected direction; no model artifact change, threshold/sizing/env change, runtime change, live switch, or restart.
- 2026-05-22 post-BUILDGUY read-only diagnostic: research summary `docs/research/20260522-post-buildguy-read-only-diagnostic/summary.md`; read-only reports `data/replay_reports/time_to_barrier_probe_20260522_post_buildguy.json`, `data/replay_reports/support_action_policy_20260522_post_buildguy.json`, and `data/replay_reports/low_volume_breakout_probe_20260522_post_buildguy.json`; external Claude sessions `917aef53-dad0-4f4e-aa20-7dd662d5828a` and `d57b1705-142f-4474-8f79-3951c948f3f9`. Since the BUILDGUY nonce failure at `2026-05-22 17:46:18`, the live canary produced `952` signal decisions with `950` rejects and `2` queued signals: BUILDGUY failed with stale nonce, while `龙爪` passed the current primary gates (`prob=0.9924232830169161`, `PredReturn=59.65499868749451`, `volume_30s=3.172212385073328`, `price_volatility=0.2425841856252674`) and closed by `TRAILING_STOP` after `33.49s` for `+0.0000303781` BNB. `龙爪` reached `+46.33%` MFE versus signal and `+41.42%` versus entry, confirming the current v95 canary can catch a clean short runner; it does not justify loosening gates. The refreshed time-to-barrier probe over `40` rejected candidates found `4` fast-profit, `2` fast-profit-then-collapse, `1` slow-runner, `11` stop-first, and `22` flat-timeout cases; `support_action_policy` still found `0` eligible rules at `min_selected=5`. The low-volume probe over `8` candidates split into `3` fast-profit-then-stop, `4` fakeout, and `1` flat case, so low-volume/short-spike evidence remains mixed. Decision: no model artifact change, no threshold or sizing change, no runtime change, no restart, and no scoreboard model-direction change beyond recording this diagnostic.
- 2026-05-22 BUILDGUY queued buy execution failure: research summary `docs/research/20260522-buildguy-buy-execution-failed/summary.md`; external Claude session `126dcebd-6c94-4e43-996f-00b38a1d122f`. After the prior `17:40:54.307910 +0800` abstention closeout, the live v95 canary produced one queued signal: `BUILDGUY` (`0x18C412942775bff9523B29752A08444986654444`) at `17:46:18.174736`, with `prob=0.9829137733278135`, `PredReturn=47.50809044519591`, `volume_30s=1.6957425722970296`, `price_volatility=0.146165426354468`, and `age=145s`. The bot used fresh lifecycle fast status (`stale=0.018s`, `chain_lag=2.176s`) and attempted a real buy for about `0.000294 BNB`, but `src.core.trader` rejected it at `17:46:19.143` with `nonce too low: next nonce 869, tx nonce 867`. No BUILDGUY row was added to `data/paper_trades.jsonl`, `data/bot_state.json` still had `positions={}`, and no later BUILDGUY `BUY SIGNAL` or open-position audit appeared. Lifecycle attribution showed the signal was already `+55.93%` from first observed price, peaked only another `+4.44%` above signal by `17:46:20`, and was `-27.32%` versus signal by the `17:55:56` runtime snapshot; the model re-evaluation after the failed buy quickly deteriorated to `PredReturn=2.7276` at `17:46:22` and then negative. Decision: classify as a transient execution-layer nonce-staleness failure, not a successful trade and not a model threshold miss. No model artifact, `.env`, threshold, sizing, trading-logic, or restart change; continue watching nonce failures as an execution reliability risk.
- 2026-05-22 post-17:05 abstention plus cleanup audit: research summary `docs/research/20260522-live-no-trade-abstention/summary.md`. Closeout snapshot through `17:40:54.307910 +0800`: since `16:48`, the live v95 canary produced `791` signal decisions, all rejected, with `0` queued buys, `0` opened buys, and `0` new paper-trade rows; since `17:05`, there were `258` additional rejected decisions and still no buys. The later candidates `🤣`, `以太坊思维`, `TER`, `108`, `币安黑奴`, `EHD1000`, `请自行做好研究`, `NNB`, and `田螺姑娘` all stayed below the signal-time PredReturn level needed to justify changing thresholds. The reported "new trade" was the earlier `吃饱饱赚饱饱` `TIME_EXIT` cleanup at `13:52:09`: the bot attempted to sell the already-open `13:42:48` position, found on-chain token balance `0`, and removed it from state; this was an operational zero-balance cleanup, not a new model entry. Because that bot process started at `13:28:59`, before `6142cee` (`fix: persist zero-balance sell cleanup`) was available to the running process, `paper_trades.jsonl` still has the `OPEN` row without a matching synthetic `CLOSE`; current source already records future zero-balance removals in `signal_audit`, and no retroactive synthetic close is added. `AIBNB` remains the main replay watchpoint from the broader slice: it reached about `+65.57%` from first-signal price and then fell about `-51.80%` from peak, so it stays a local borderline case rather than a public model change. Decision: no model artifact change, no threshold/sizing change, no runtime change, no restart, and no scoreboard model-direction update beyond this abstention/cleanup note.
- 2026-05-22 live watchpoint after `16:36 +0800`: research summary `docs/research/20260522-live-watchpoint-wenzhou/summary.md`. The live v95 canary produced `166` signal decisions through `16:48`, with `0` queued buys, `0` opened buys, and `0` new paper-trade rows. `温州队长` was the useful watchpoint: its first signal had `prob=0.9848170688295238`, `PredReturn=4.558862752960975`, and failed on `entry_volume_30s_below_min`; its max-prob signal had `prob=0.9902015387357727`, `PredReturn=-6.624934944561912`, and failed on `pred_return_below_min`. The path did peak `+115.54%` after first signal, but by the closeout snapshot it was about `-62.38%` from peak. Decision: no live action, no model artifact change, no `.env` change, no threshold/sizing change, and no bot restart. Keep this as a replay watchpoint for volume-gated missed-runner analysis, not as evidence to lower volume or PredReturn gates.
- 2026-05-19 flow activation/fakeout probe: live-first evidence compared the clean profitable `赵长娥` trade against recent TSG and x402 failures. SmartSearch research is in `docs/research/20260519-flow-activation-fakeout/summary.md`; the read-only implementation is `src/pipeline/flow_activation_probe.py` plus `scripts/probe_flow_activation.py`; report is `data/replay_reports/flow_activation_probe_20260519_v97.json`. The report scored `521` per-token candidates from `13,423` signal events and accepted only one retrospective candidate: `赵长娥`, classified as `flow_activation_clean_profit` with volume ramp ratio `2.91`, pre-buy pressure `1.0`, and first `+25%` in about `6.13s`. `币安 x402` was classified as `dead_flow_rescue`, and TSG was skipped because PredReturn trajectory did not pass the ramp gate and the path hit stop-loss first. Decision: promote the flow-trajectory and dead-flow-rescue parts to a replay-integrated candidate gate or conditional-exit experiment, but do not treat sell-pressure fakeout as proven yet because the report has `sell_pressure_fakeout=0`. Do not switch live from this probe alone. The report is explicitly `live_switch_evidence=false`, `safe_for_live_gate=false`.
- 2026-05-19 late-pump exhaustion veto replay: live TSG and x402 both supported a path-state exhaustion hypothesis. TSG was an old late chase (`age≈210s`, `PredReturn=39.56`, STOP_LOSS), while x402 was a young peak-then-fade entry that peaked about `+158%` before signal and closed by TIME_EXIT. Research is in `docs/research/20260519-late-pump-exhaustion-veto/summary.md`; implementation is replay-only in `src/pipeline/train_hybrid.py` plus `scripts/run_late_pump_exhaustion_replay.py`; report is `data/replay_reports/late_pump_exhaustion_replay_20260519_v95.json`. The bounded 16-candidate grid used 10% sizing and selected candidate `0`, but validation had `late_pump_veto_reject_count=0` and no improvement over baseline; final confirmation also failed profit/stress gates. Decision: reject and do not switch live. Keep the evidence for richer candidate-level path-state labels or conditional exits, not another broad veto or threshold relaxation.
- 2026-05-19 low-volume breakout/fakeout probe: SmartSearch-backed breakout/fakeout and meta-labeling research led to a read-only probe for v95 `entry_volume_30s_below_min` rejects, saved as `data/replay_reports/low_volume_breakout_probe_20260519_v95.json` with implementation in `src/pipeline/low_volume_breakout_probe.py` and CLI `scripts/probe_low_volume_breakout.py`. Since the v95 restart at `2026-05-19 04:02:23`, the report saw `2680` rejected signal decisions, filtered `26` low-volume decisions, and deduplicated to `16` per-token candidates. Classes were `low_volume_runner=2`, `low_volume_fast_profit_then_stop=5`, `low_volume_fakeout=7`, and `low_volume_flat=2`; policies were `conditional_rescue_probe=2`, `quick_take_profit_probe=5`, and `skip=9`. This confirms the low-volume blocked pocket is mixed: `微信时刻` and `HERMANO` were clean low-volume runners, while `MATRIX-3`, `PI-402`, `币安社区`, `尼罗基金会`, `小鸟咪`, `4lpha`, and `Agora-1` were stop-first fakeouts. Decision: promote this direction to a replay-integrated conditional low-volume rescue plus quick-profit/conditional-hold experiment, but do not switch live and do not lower `MIN_ENTRY_VOLUME_30S` globally. The report is explicitly `live_switch_evidence=false` and keeps the 10% sizing policy unchanged.
- 2026-05-19 time-to-barrier probe: SmartSearch-backed time-to-profit/competing-risk research led to a read-only first-barrier probe over the v95 live window, saved as `data/replay_reports/time_to_barrier_probe_20260519_v95.json` with implementation in `src/pipeline/time_to_barrier_probe.py` and CLI `scripts/probe_time_to_barrier.py`. Since the v95 restart at `2026-05-19 04:02:23`, the probe saw `2042` rejected signal decisions, deduplicated to `66` token candidates, and recorded run-time SHA-256 fingerprints for the live input files. Each input is read once into a bytes snapshot, so hashes and parsing use those same bytes; current collector/bot input paths may change after the report is written. Classes were `fast_profit=12`, `fast_profit_then_collapse=8`, `slow_runner=1`, `stop_first=7`, and `flat_timeout=38`; policies were `quick_take_profit=20`, `conditional_slow_hold=1`, and `skip=45`. Under a stricter sanity filter of `prob>=0.94` and `PredReturn>=0`, `11/19` candidates were quick-take-profit, `1/19` was slow-runner, and `7/19` were skip. Decision: promote this direction to a replay-integrated conditional entry/exit experiment, but do not switch live. The report is explicitly `live_switch_evidence=false`; the next test must keep 10% sizing, preserve v95/v84 as the primary candidate generator, and reject the direction if validation/final/walk-forward/stress fail versus the current best v95 baseline.
- 2026-05-19 rare-runner candidate ranker probe: SmartSearch-backed ranking research led to a read-only `CatBoostRanker` / `YetiRank` probe over the existing v95 candidate set, saved as `data/replay_reports/v96_candidate_ranker_probe_recent4_20260519.json` with supporting implementation in `src/pipeline/candidate_ranker_probe.py`. The full-file attempts were stopped because rebuilding all lifecycle samples on the live machine used more than 5GB RAM for several minutes, so the falsification used four explicit closed lifecycle files; the report records the exact lifecycle file paths and SHA-256 fingerprints. Validation improved over the existing entry-value ordering (`45.0` vs `41.0` top relevance, clean runners `12` vs `9`, collapses `1` vs `1`), but final was worse (`60.0` vs `61.0`, clean runners `15` vs `15`, collapses `5` vs `4`). Do not switch live or integrate this ranker into replay/live. Treat the result as evidence that a generic YetiRank layer on the current entry features is too weak; the next model direction should use live path state more directly, such as conditional exit/re-entry logic around STOP_LOSS or PPO runner-retention events.
- 2026-05-19 stop-loss re-entry / runner-retention probe: a new read-only probe over `paper_trades`, `signal_audit`, collector runtime state, and latest lifecycle incrementals found a narrow conditional-exit opportunity, not a live-switch candidate. Latest run `data/replay_reports/reentry_retention_probe_20260519_050349.json` classified `6` STOP_LOSS re-entry candidates with `1` accepted (`币安小子`: `+25` in about `18.1s`, `+60` in about `53.1s`, MFE about `+141.2%`) and `6` PPO runner-retention candidates with `1` accepted (`何赵`: `+25` in about `31.0s`, MFE about `+34.8%`). It correctly rejected `WAGMI` and `B402`; `BISMILLAH` reclaimed quickly but hit `-25` inside the post-reclaim guard window, so it is treated as fake-runner risk. This supports a replay-level conditional re-entry / runner-retention experiment, not global threshold lowering, blanket longer hold, or blanket partial exits.
- 2026-05-19 replay/live alignment fix: `model_replay.live_replay_config_from_manifest` and `train_hybrid._run_eval_replay` now preserve the manifest near-threshold rescue gate (`buy_near_*`) through runtime, all-in, stress, and walk-forward replay. The corrected v95 final replay matches the accepted v95 manifest exactly on headline metrics (`511.4778%` return, `0.03067113` BNB profit, `44` trades), with `near_threshold_entry_count=3`. Treat any earlier replay report missing `buy_near_threshold_min_prob` as a v84-like replay, not a valid v95 replay.
- 2026-05-19 selective near-threshold probe accepted v95 as a canary candidate. Live evidence came first: no new real trade after WAGMI, but high-score rejects showed `ERNIE` as a clean missed runner (`prob=0.9490`, `PredReturn=35.1430`, `volume_30s=1.2743`, MFE `+53.189%`) while two `BNBROCK` near-misses were correct skips. The implemented rule is deliberately narrow: keep v84 primary threshold at `0.98`, add only a `0.94-0.98` rescue band with `entry_score>=32`, `volume_30s>=1.25`, `price_volatility>=0.08`, and the existing max-age bound. This is structurally different from the rejected global `0.95` threshold sweep because the primary threshold remains unchanged and the rescue gate is manifest-scoped to v95. Risk note: the validation sweep did not discriminate strongly (`144/144` rules passed; top rule had only `2` near episodes and `0` trade delta), and the final improvement is one extra trade, so live canary attribution is mandatory before promoting v95 to durable baseline.
- 2026-05-19 01:40 live-first cycle: bot and collector were healthy under `memectl`/tmux, `.env` still used live v67 with 10% sizing, state had `0` open positions and `0.005079303120051795` BNB balance. There were no real trades after the WAGMI close. Since WAGMI close there were `2493` rejected signal decisions: `2470` `pred_return_below_min` and `23` `buy_model_reject`. The newest high-PredReturn near miss, `FELON`, peaked at `PredReturn=42.0238`, `prob=0.9536`, and `volume_30s=1.4657`, then only reached about `+6.99%` MFE and hit the `-18%` stop zone about `68s` after the signal. `Scamlon`, `RVM`, and `ELONG` also showed no runner path. This confirms the recent live issue is still narrow separation: most high-looking rejects are correct skips, while `xPBNB` remains the clean missed-runner outlier. The next experiment should be the already researched v84-like candidate-level runner/collapse gate, not global threshold lowering, volume relaxation, blanket partial exits, or broader profit-path training.
- v75b used `profit_path` behavior cloning, `min_policy_hold_seconds=60`, `trailing_start_pct=0.35`, `trailing_stop_pct=0.12`, `stop_loss=-0.25`, `entry_delay_seconds=1`, `exit_delay_seconds=4`, and 10% position sizing.
- v75b is useful evidence that simply forcing longer minimum hold is not enough. The next promising direction is conditional exit logic: keep runners longer only when path/flow features justify it, while preserving fast exits for collapsing tokens.
- v76 is useful evidence that making the buy label stricter is not enough. Recent near-miss analysis found a small runner subset among high `PredReturn` rejects, but most high `PredReturn` rejects still had large downside. The next direction should be a second-stage runner filter or conditional exit policy, not a global threshold relaxation.
- v77 confirms the same shape with a softer threshold: some runner-like cases exist in high `PredReturn` rejects, but a single global quality threshold still leaves too much money on the table versus v67. The next step should be a dedicated runner classifier or conditional exit path, not another threshold sweep.
- v78 confirms that using average-path return as the primary buy label is too restrictive. It raises apparent win rate but collapses trade count and turns both walk-forward and stress replay negative, so the next candidate should separate runner discovery from dump avoidance instead of replacing the main buy label with an average-path target.
- The latest-calibrated v67 `min_entry_score=35/40` sweep confirms entry-value gating is useful as a risk filter but not sufficient as a standalone improvement. It improves validation and harsh-stress robustness, but final return stays below the accepted hold75 baseline.
- A quick validation falsification of lower thresholds showed that `buy_threshold=0.95/0.94/0.93` plus simple young-token, volume, and volatility filters admits too many weak signals. The `0.95` and `0.94` variants lost money with extreme drawdown, while `0.93` needed 1281 validation trades and still had `-64.4315%` max drawdown. Do not lower the global threshold without a learned second-stage gate.
- A 2026-05-17 post-restart near-miss check again rejected global threshold lowering: seven `prob>=0.93` rejects with available 300s paths mostly had only `0-8%` max upside before falling `-25%` to `-59%`; the best short spike (`CC`) reached about `+34%` but later faded to about `-22%`. This points to conditional runner/exit modeling, not broader entry.
- Early trailing was falsified as a simple fix for short spikes. On the current latest-calibrated validation split, v67 hold75 baseline returned `195.7659%`; `trailing=30%/10%` returned `183.0245%`, and `trailing=25%/10%` returned `178.6817%`. Do not switch to globally earlier trailing.
- Research note: Hudson & Thames' meta-labeling writeup frames the method as a secondary model that learns when to use a primary model and describes triple-barrier labels as path-based labels rather than simple next-step direction labels. For this repo, the next viable experiment should preserve v67 as the primary model and train a second-stage take/skip or runner classifier on v67 candidate signals, using path/barrier outcomes and live execution costs. Source: https://hudsonthames.org/does-meta-labeling-add-to-signal-efficacy-triple-barrier-method/
- Future research notes must be reproducible. When a rejected or accepted model is influenced by outside research, create `docs/research/<YYYYMMDD>-<slug>/`, run `smart-search deep` first to generate `plan.json`, execute the planned SmartSearch discovery/fetch commands, and record fetched source links plus the key commands that influenced the experiment.
- In this scoreboard, "search", "research", "look up", "查找资料", "网上调查", and "深度搜索" mean SmartSearch Deep Research: `smart-search deep` plan first, then planned `smart-search search`/`exa-search`/`zhipu-search`/`context7-*`/`map`/`fetch` evidence. A standalone broad search result is discovery only, not proof.
- Do not use native Codex web browsing or uncited model memory as evidence for model-method decisions. If SmartSearch is unavailable, record the blocker; do not silently downgrade to ordinary search.
- Do not cite broad search summaries as proof in this scoreboard. If a claim changes model structure, labels, features, exits, or live deployment logic, it needs a fetched source saved under `docs/research/` and a `summary.md` explaining how the evidence changed the experiment.
- v67 hold60 is accepted as an exit-parameter upgrade, not a new buy model. A same-data check versus hold40 showed `267.5674%` vs `259.4872%` final return, `64.5455%` vs `59.0909%` win rate, `-21.0330%` vs `-23.7954%` max drawdown, `41.1429%` vs `30.1828%` walk-forward worst return, and stronger harsh stress replay. This matches live evidence that some profitable runners were being cut too quickly, while keeping the hard stop, trailing, and 10% sizing unchanged.
- v79 shows that a simple `live_target_hit_before_stop` barrier label with a `25%` target is too permissive for this data. It generated many low-quality entries and failed even after raising the threshold, so the next profitable direction should preserve v67's high-quality entry model and focus on conditional runner exits or second-stage filtering rather than replacing the buy target wholesale.
- v67 hold75 is accepted as a latest-calibration exit-parameter upgrade, not a new buy model. Under 2026-05-17 execution calibration (`entry_delay=1`, `entry_max_fill_wait=4`, `entry_price_protection=7.9784%`, `exit_delay=7`, `exit_max_fill_wait=8`) and explicit `max_hold_seconds=300`, validation improved from hold60 `182.7473%` to `195.7659%` return, `61.0000%` to `63.0000%` win rate, `44.0271%` to `48.8485%` walk-forward worst return, and harsh execution from `-0.5943%` to `15.7276%`. Final return improved from `187.7406%` to `194.7110%`; final drawdown and walk-forward were slightly worse but remained within gate, with harsh stress still positive.
- v80 shows that token-balanced sample weighting is not a standalone meta-labeling fix. It can surface many more trades and inflate headline return, but without a strict second-stage gate it overfits active token clusters and destroys final walk-forward robustness. Any future token-balanced experiment must constrain the primary threshold/trade count or use it only inside a true v67-candidate take/skip gate.
- A 2026-05-18 fast falsification of `buy_sample_weighting=recency_decay` on the target-hit60 / entry-value stack did not beat v84. With `max_samples_per_token=20`, `buy_recency_half_life_hours=36`, and `catboost_iterations=100`, the holdout run reached `65.8447%` final return, `-10.3151%` max drawdown, `4.2660%` WF worst return, `22` trades, `5.6425%` harsh friction, and `-6.9837%` harsh execution. The implementation is useful, but the current weighting curve is not a baseline improvement.
- v67 `entry_value` / `min_entry_score=40` is accepted as a runtime gate upgrade, not a new buy model. It uses the already shipped diagnostic entry-value model as the live `PredReturn` filter. On the 2026-05-18 current-data full validation it improved return from `193.4369%` to `201.5949%`, win rate from `63.0000%` to `66.2162%`, and WF worst return from `47.8686%` to `64.1369%`. On final it improved return from `187.9662%` to `189.6565%`, win rate from `61.9565%` to `67.6923%`, max drawdown from `-21.5063%` to `-15.6950%`, and harsh execution from `13.6437%` to `35.1696%`. The earlier 2026-05-17 `min_entry_score=40` rejection was under the older same-data hold75 check without this final current-data acceptance pass.
- A follow-up entry-protection sweep on the same v67 runtime gate showed that widening `entry_price_protection_pct` materially improves live-sized replay and stress. The best nearby offline compromise was `entry_price_protection_pct=0.18` with `min_entry_score=45`, which reached `220.2125%` final return, `-16.7280%` max drawdown, `40.3773%` WF worst return, and positive harsh stress (`74.6735%` harsh execution, `63.4903%` harsh friction). It is better than the accepted `min_entry_score=40` runtime gate on return, walk-forward, and stress, but the live bot was left unchanged per user instruction and should be restarted into this candidate later.
- The latest v84 hold-window sweep promotes `max_hold_seconds=560`, not `420`, `480`, `540`, `580`, or `600`. The 560s point has the best final return/profit plus stronger harsh friction and harsh execution than 540s, while 580/600 both lose stress robustness.
- The v84 `min_policy_hold_seconds=60` transfer test was rejected. The v86b-specific hold60 improvement does not generalize to the stronger v84 model; v84 stays at `min_policy_hold_seconds=45`.
- The v86b delay-robust probe confirms that path-aware labels and profit-path behavior cloning can improve over the live v67 baseline, but not enough to beat v84. Within v86b, the profitable adjustment was increasing `min_policy_hold_seconds` from `45` to `60` while keeping `min_entry_score=35`; raising `min_entry_score` improved risk but cut too much return, and shortening the hold floor to `20` degraded both return and drawdown.
- A 2026-05-18 live near-miss attribution pass falsified simple entry-volume relaxation as the next v84 improvement. In the latest four-hour signal audit window, high `PredReturn` rejects mostly weakened or hit stop-loss conditions. A local live-delay path simulation of `pred_return>=40` candidates showed the current live-style gate (`volume_30s>=1.5`) would have selected 9 simulated entries with only `22.2%` win rate and about `-66%` summed gross return across candidates; relaxing to `volume_30s>=1.25` or `>=1.0` did not create a runner set and increased collapse exposure. Keep v84's `min_entry_volume_30s=1.5` until a learned second-stage runner/collapse classifier proves otherwise.
- v87b falsified the idea of using strict delay-robust return directly as the `entry_value` live gate. The final split had signals but no trades because every candidate failed `min_entry_score=35`; this protects capital but cannot improve live profitability. Keep delay-robust path data as a feature/label component, but the next model should learn a softer take/skip meta-label on plausible primary candidates.
- v88 shows that simply softening the delay-robust regression target to average delayed return is still not enough. It can be made to trade at lower thresholds, but the resulting basket has much lower win rate, much worse drawdown, and far weaker walk-forward return than v84. The evidence points to a targeted runner/collapse meta-label, not another broad threshold or volume relaxation.
- v89 shows that pushing the target-hit barrier up to 80% still does not beat v84. It improves over v88 on validation and final return, but the final set remains below v84 and stress collapses hard. The next step should be a dedicated runner/collapse gate or a conditional exit model, not a stricter primary target.
- v91 shows that a raw binary runner-probability `entry_value` gate is not enough. At live-useful thresholds it is too sparse, while lower thresholds explode into 1400+ final trades with `-56%` to `-61%` drawdown and near-`-100%` walk-forward worst return. The live failure pattern is still early fake runners, but the next model needs candidate-level meta-labeling or conditional exit structure rather than replacing the regression gate with a raw binary hit-before-stop score.
- v93 shows that token-balanced sampling is still not enough even when the runtime threshold and entry-value gate are constrained back toward the v84 stack. It recovers trade count and headline return versus v91, but stress robustness and validation walk-forward remain weak, so this line should not replace v84. The next iteration should focus on conditional exits or a stricter candidate-level meta-label built around v84-style primary candidates.
- The partial-exit runtime sweep on v84 confirms the same direction: the existing PPO policy can harvest more runner value when `allow_partial_exits=True`, but the validation replay weakens enough that it should not replace the baseline as-is. Treat it as evidence that a trained conditional-exit model is worth building, not as a blanket live toggle.
- The 2026-05-19 v84 near-threshold sweep confirms that missed runners are real but not separable by a global threshold relaxation. Lowering `buy_threshold` to `0.95` captured enough final runners to lift final return to `498.6497%`, but validation remained weak at `134.5177%` return and `-31.7694%` walk-forward worst drawdown. This should feed a second-stage runner/collapse classifier, not a live threshold cut.
- 2026-05-18 live attribution adds two separate failure shapes. The current live v67 stack bought `WAGMI` (`0x91d1AAcA03B789D2Df93A0857655aDE813A44444`) at `21:41:57`, with `prob=0.9739`, `PredReturn=52.89`, favorable entry slippage, and fast lifecycle status. It still closed by `STOP_LOSS` after `21.29s` for `-0.00018272` BNB. The lifecycle path shows post-entry MFE of about `+18.20%` by `21:42:02`, then MAE of about `-30.65%` by receipt exit. This is not a speed failure; it is evidence for a conditional early-profit/runner-exit policy that can harvest brief rebounds before the stop cascade.
- The same 2026-05-18 high-PredReturn rejection sweep found one major missed runner, `王福满` (`0x78df06C3C48c63E1FA51e9223EFC004f8E844444`): it was rejected around `21:36:35-21:36:40` because `PredReturn`/buy probability were just under the live gates, yet the 300s path reached roughly `+346%` to `+476%` MFE and stayed strongly positive. However, among `21` rejected signals with `PredReturn>=35` and available paths, `19` ended below the signal path base and `13` had MAE worse than `-25%`. Do not lower the global threshold; use this as evidence for a narrow second-stage "missed runner" detector with strict risk gates.
- v94 confirms that training a broad profit-path partial-exit policy is still too permissive. The live-first pass before the run found WAGMI was already below the -18% stop zone about 4 seconds after signal, while most high-PredReturn rejects after WAGMI also hit -18% or worse; xPBNB remained the rare clean missed runner. v94 did not learn that narrow separation: calibration selected a fallback `0.941` threshold, final win rate fell to `26.2799%`, validation walk-forward worst return fell to `-68.9524%`, and stress replay was strongly negative. Next work should avoid blanket partial-exit/profit-path training and instead build a candidate-level runner/collapse filter anchored to v84-like primary candidates plus live path features.
